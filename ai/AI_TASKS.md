# Chatbot AI — Danh sách việc

> Phạm vi: `ai/**` (model, RAG, training, server suy luận) + phần nối mỏng phía `web/` (`web/src/app/chat/`, `web/src/app/api/chat/`). Chi tiết đầy đủ (model chọn, lý do, cách sinh dữ liệu train, chia mốc) nằm ở [`AI_CHATBOT_PLAN.md`](../AI_CHATBOT_PLAN.md) và `PROJECT_CONTEXT.md` mục 11 — file này chỉ theo dõi *việc nào xong, việc nào chưa*. Đọc `PROGRESS.md` ở gốc dự án trước để biết trạng thái tổng quan.

## Milestone 6 — Hạ tầng + RAG (đang làm)

- [x] Dời app Next.js vào `web/`, tạo bộ khung `ai/`, sửa mọi path/config/script/tài liệu liên quan
- [x] Tự kiểm sau khi dời: `npm install` + `typecheck` + `lint` + `test` (148/148) + `test:e2e` (8/8) — tất cả xanh trong `web/`, việc dời không làm hỏng gì. Cài thêm Visual Studio Build Tools (workload C++) trên máy dev vì `better-sqlite3` cần biên dịch native và máy chưa có sẵn.
- [x] Cài Ollama (chạy model local qua HTTP API riêng, port 11434 mặc định) — dùng thay vì tự dựng llama.cpp thủ công, đơn giản hơn cho user chưa quen LLM
- [x] `ollama pull qwen3.5:4b` (3.4GB) + `ollama pull nomic-embed-text` (274MB) — cả 2 xác nhận có sẵn trên thư viện Ollama chính thức, tải xong thật trên máy user
- [x] Thử câu hỏi tiếng Việt qua `/chat` thật — chất lượng tốt, trả lời đúng và mạch lạc (xem ví dụ ở mục 11.4)
- [x] Route `POST web/src/app/api/chat/route.ts` — validate input, đọc `getChatContextSummary()` (DB của `web/`), gọi sang `ai/server/`, map lỗi 503/502 rõ ràng, timeout 120s (xem phát hiện độ trễ bên dưới). Tự kiểm: `typecheck`/`lint` xanh.
- [x] Trang `web/src/app/chat/page.tsx` + `ChatWindow.tsx` (có ghi chú "có thể mất khoảng 1 phút" khi đang chờ), thêm "Chatbot" vào Nav. Component test `ChatWindow.test.tsx` (5 case) — 153/153 test xanh.
- [x] `ai/server/` (Python/FastAPI): `main.py` (route `/chat` + `/health`), `ollama_client.py` (gọi Ollama `/api/chat` + `/api/embeddings`, `think: false` — xem phát hiện bên dưới), `chunking.py` + `retrieval.py` + `prompt.py` (RAG thuần, có test pytest không cần model thật — 19 test, xanh), `build_index.py` (chunk+embed `PROJECT_CONTEXT.md`/`USER_GUIDE.md`/`document.txt` → `ai/data/processed/doc_index.json`, 139 đoạn)
- [x] `ai/requirements.txt` + `ai/README.md` (hướng dẫn chạy) + `ai/start-server.bat` (script 1 chạm, theo đúng kiểu `start-app.bat` đã có)
- [x] Nối thật: chạy `ai/server/` + Ollama, thử `/chat` từ server thật (không phải mock) với câu hỏi về công thức Band — trả lời đúng, có trích dẫn đúng nội dung `PROJECT_CONTEXT.md`
- [x] Thêm e2e case cho luồng chat (`chat.spec.ts`) — dùng **AI server giả lập** (`mockAiServer.ts`, `playwright.config.ts` giờ chạy 2 `webServer` song song) thay vì Ollama thật, để bộ e2e không phụ thuộc máy khác có cài Ollama hay không và không bị chậm/không ổn định — xem "Phát hiện quan trọng" bên dưới về lý do
- [x] Tự kiểm cuối: `typecheck`/`lint` sạch, `test` 153/153, `test:e2e` 9/9 (thêm `chat.spec.ts`) xanh trong `web/`; `pytest` 19/19 xanh trong `ai/`
- [x] Review chốt Milestone 6 (Supervisor, theo `CLAUDE.md`) — 1 finding nhỏ (doc lỗi thời: `PROJECT_CONTEXT.md` mục 11.4 còn ghi "60s" thay vì "120s"), đã sửa, không cần vòng review thứ 2. **MILESTONE 6 XONG.**

### Phát hiện quan trọng khi nối thật (đọc trước khi đụng vào `ai/server/ollama_client.py`)

1. **Qwen3.5 bật "thinking mode" mặc định** — model tự sinh cả đoạn suy luận dài trước khi trả lời, kể cả câu hỏi cực đơn giản. Đo trực tiếp trên máy user (RTX 3050 4GB): cùng 1 câu hỏi ngắn mất **73.7 giây** (thinking bật) so với **1.1 giây** (thinking tắt qua tham số `"think": false` trong request `/api/chat` của Ollama) — nhanh hơn ~65 lần. `ollama_client.py` đã set `think: false` cứng — **đừng bỏ dòng này** nếu sau này sửa file đó.
2. **Độ trễ thật với RAG:** 1 câu hỏi thật về công thức Band (có kèm 4 đoạn tài liệu RAG trong prompt) mất **~46 giây** kể cả khi đã tắt thinking — do model 4B chạy trên GPU 4GB + phải xử lý prompt dài hơn. Timeout đã đặt 120s ở cả `web/`'s route và `ai/server/ollama_client.py` để có biên an toàn. Đây là đặc điểm thật của phần cứng, không phải lỗi — đã ghi UI (mục "đang trả lời...") để user không tưởng bị treo.
3. **`chunk_markdown` có 1 lỗi thật đã sửa:** đoạn văn đơn lẻ dài hơn `max_chars` (không có dòng trống bên trong) từng bị bỏ qua không tách nhỏ, khiến `nomic-embed-text` từ chối với lỗi "input length exceeds the context length" khi build index thật (gặp ở 1 đoạn trong mục 9 `PROJECT_CONTEXT.md`). Đã sửa + thêm test (`test_splits_a_single_oversized_paragraph_on_sentence_boundaries` và 2 test liên quan).

## Milestone 6 mở rộng — Đổi tên "Navita" + bình chat nổi + 3 chế độ + lịch sử nhiều cuộc hội thoại (đang làm)

> User quay lại sau khi Milestone 6 đã review chốt, yêu cầu thêm 4 việc. Tính là phần mở rộng của Milestone 6 (không phải milestone số riêng) vì cùng phạm vi `ai/server/` + `web/`'s chat route/UI.

- [x] Đổi tên chatbot thành **Navita** — hệ dẫn (`ai/server/prompt.py`), welcome message, tiêu đề trang, Nav, `USER_GUIDE.md`
- [x] Schema DB mới: `chatConversations` (title tự đặt, mode lưu riêng từng cuộc) + `chatMessages` — migration `0003_rich_night_thrasher.sql`. Cập nhật `PROJECT_CONTEXT.md` mục 4.
- [x] API mới: `GET/POST /api/chat/conversations`, `GET/PATCH/DELETE /api/chat/conversations/:id`. Viết lại `POST /api/chat` để lấy lịch sử từ DB (không nhận `history` từ client nữa — tránh lệch dữ liệu giữa bình nổi/trang /chat/tải lại trang). Cập nhật `PROJECT_CONTEXT.md` mục 6 + 11.4.
- [x] 3 chế độ **Flash/Thinking/Pro** — vẫn 1 model Qwen3.5-4B, khác `think`/`top_k`/hệ dẫn (`ai/server/main.py`'s `MODE_SETTINGS`, `ai/server/prompt.py`'s `PRO_INSTRUCTION_SUFFIX`). Lưu theo từng cuộc hội thoại.
- [x] `ChatWindow.tsx` viết lại: chọn/tạo/xóa cuộc hội thoại (dropdown + nút), nút chọn chế độ, dùng chung được cho cả trang đầy đủ và bình nổi (`compact` prop)
- [x] `ChatBubble.tsx` mới — bình 💬 nổi góc màn hình, gắn vào `layout.tsx` nên hiện mọi trang trừ `/chat` (tránh trùng lặp); dùng chung `ChatWindow`/API nên đồng bộ với trang đầy đủ
- [x] Sửa 2 lỗi thật phát hiện khi làm/test kỹ:
  1. **Race condition**: tạo cuộc hội thoại mới rồi gửi tin ngay → set `activeId` kích hoạt effect tải tin nhắn (rỗng, vì cuộc mới) → ghi đè mất tin nhắn vừa gửi lạc quan trên UI. Sửa bằng `skipNextMessageFetchForIdRef` — bỏ qua đúng 1 lần fetch cho cuộc vừa tự tạo cục bộ.
  2. **Timeout quá ngắn cho Thinking/Pro**: test thật chế độ Pro (think bật + top_k=6, tổ hợp chậm nhất) gặp `httpx.ReadTimeout` ở ~122s vì `ollama_client.py` đặt timeout 120s chung cho cả chat lẫn embed. Tách riêng `EMBED_TIMEOUT=30s` (embedding luôn nhanh) và `CHAT_TIMEOUT=300s`; nâng `AI_SERVER_TIMEOUT_MS` phía `web/` lên 300s cho khớp.
- [x] Sửa lint `react-hooks/set-state-in-effect` (React Compiler) — hiệu ứng tải danh sách/tin nhắn ban đầu phải tự chứa logic `setState` trong `.then()` ngay trong thân effect, không được gọi ra 1 hàm `useCallback` riêng rồi hàm đó mới `setState` (dù hàm đó là async) — linter coi đó là "effect gián tiếp gây setState đồng bộ".
- [x] Xác nhận Flash (30.5s) + Pro (244s, có bật thinking + top_k=6) chạy được thật với model thật (không phải mock), cả 2 đều tự giới thiệu đúng là "Navita", Pro đúng phong cách trang trọng/có cấu trúc như thiết kế
- [x] Tự kiểm cuối: `typecheck`/`lint` sạch, `test` 156/156, `test:e2e` 9/9 xanh trong `web/`; `pytest` 22/22 xanh trong `ai/`
- [x] Xem lại giao diện thật bằng Playwright (script tạm, đã xóa sau khi dùng) — chụp màn hình trang chủ (bình nổi đúng vị trí), bình nổi mở ra (welcome message + 3 nút chế độ + dropdown hội thoại), trang `/chat` đầy đủ (không hiện bình nổi trùng lặp, đúng như thiết kế mục 11.1.1) — không có lỗi console
- [x] Review chốt (Supervisor) phần mở rộng này — **5 finding thật, đã sửa hết, không phải chỉ ghi chú:**
  1. **[Nghiêm trọng]** Xóa cuộc hội thoại đang chờ trả lời → `addChatMessage` (assistant) insert vào cuộc đã xóa → SQLite chặn bằng FK constraint (`foreign_keys = ON`) → request crash 500, mất câu trả lời AI dù đã tốn GPU. Sửa: bọc try/catch ở `route.ts`, vẫn trả `reply` cho client dù lưu DB thất bại.
  2. **[Nghiêm trọng]** Chuyển sang xem cuộc hội thoại khác trong lúc đang chờ trả lời (Thinking/Pro tới ~4 phút, đủ thời gian thao tác) → câu trả lời "rò" vào cuộc đang xem + tự động kéo user quay lại cuộc cũ không báo trước. Sửa: khóa dropdown/nút "+"/nút xóa khi `isSending` (chặn từ UI) + `activeIdRef`/`setActiveIdAndRef` kiểm tra đúng cuộc trước khi cập nhật `messages`/lỗi (chặn ở tầng logic) + `refreshConversations()` không còn ép đổi `activeId`.
  3. **[Trung bình]** Bấm Gửi/Enter rất nhanh 2 lần trước khi `isSending` (state) kịp render → tạo trùng 2 cuộc hội thoại + gửi trùng. Sửa: `isSendingRef` (ref đồng bộ, không phụ thuộc chu kỳ render) kiểm tra ngay đầu `handleSend`.
  4. **[Nhỏ]** `PROJECT_CONTEXT.md` mục 11.4 còn ghi "timeout 120s" dù code đã tăng lên 300s — đã sửa.
  5. **[Nhỏ]** Test mới chưa phủ 3 race condition trên — đã thêm 2 test (`ChatWindow.test.tsx`): khóa nút khi đang chờ, không gửi trùng khi bấm 2 lần liên tiếp (dùng `fireEvent.click` bắn 2 lần không chờ giữa chừng để mô phỏng đúng race thật, không dùng `userEvent` vì nó tự chờ React render giữa các thao tác nên không tái hiện được race).
  
  **Tự sửa 1 lỗi phát sinh khi vá Finding 2**: lần sửa đầu dùng `useEffect` để đồng bộ `activeIdRef` theo `activeId`, nhưng `handleSend` vừa tạo cuộc hội thoại (set `activeId`) vừa cần đọc `activeIdRef` đã cập nhật ngay sau đó trong cùng 1 lần gọi — không có gì đảm bảo effect đã chạy kịp. Phát hiện qua 3 test tự dưng đỏ, sửa bằng cách gán `activeIdRef.current` trực tiếp, đồng thời với mọi lần gọi `setActiveId` (qua hàm `setActiveIdAndRef`), không qua effect nữa.
  
  Tự kiểm lại sau khi sửa: `typecheck`/`lint` sạch, `test` 158/158, `test:e2e` 9/9 (web/), `pytest` 22/22 (ai/) — tất cả xanh. **MILESTONE 6 MỞ RỘNG XONG.**

## Milestone 7 — Fine-tune (đang làm)

> User xác nhận (2026-09-19): đã có tài khoản Kaggle; đồng ý ranh giới không đưa dữ liệu cá nhân lên Kaggle; số lượng mẫu mục tiêu ~200–300 cặp giữ nguyên như kế hoạch. Cách xem mẫu duyệt trả lời ngắn gọn ("Thôi") — hiểu là dùng mặc định (hiện trong chat), đã nói rõ lại với user trước khi làm.

- [x] Soạn template câu hỏi tự động điền số liệu từ tài liệu dự án — `ai/training/generate_template_data.py` (+ `markdown_sections.py` thuần, 6 test), quét `PROJECT_CONTEXT.md`/`USER_GUIDE.md` theo mục đã chọn lọc (chỉ mục user-facing, bỏ mục dev-facing như schema/API/test strategy), cộng parse trực tiếp mục "Câu hỏi thường gặp" (đã sẵn dạng Q&A). Ra **26 cặp** (ít hơn ước tính 40–60, chấp nhận được — chất lượng hơn số lượng). `clean_answer()` **chỉ làm sạch cú pháp** (link markdown, dấu `---` thừa) — cố tình **không** rút gọn/viết lại nội dung, vì làm vậy tức là Claude "sáng tác" nội dung train, đúng điều bị cấm ở mục 6. Kết quả: một số câu trả lời dài/kỹ thuật (lẫn code, ghi chú dev) vì trích nguyên văn tài liệu — sẽ để user tự đánh giá ở cổng duyệt, không tự ý "sửa cho hay".
- [x] Viết notebook Kaggle sinh dữ liệu bằng Qwen3.5-9B (KHÔNG dùng Claude) — `ai/training/build_kaggle_notebook.py` sinh ra `ai/kaggle/generate_dataset_kaggle.ipynb` (gitignore, tái tạo bằng script; `ai/kaggle/` là nơi chứa mọi notebook để tải lên Kaggle, tách khỏi code Python nguồn ở `ai/training/`), nhúng sẵn dữ liệu đoạn tài liệu (đã strip embedding, chỉ giữ text) nên user chỉ cần tải notebook lên Kaggle chạy, không cần upload gì thêm. Có cell "smoke test" 2 đoạn trước khi chạy hết (đỡ tốn GPU nếu có lỗi).
- [x] **Lỗi thật phát hiện qua chính smoke test user chạy trên Kaggle** (không phải giả định): bản đầu nhúng cả 139 đoạn (không lọc) — smoke test ra 1 cặp hỏi-đáp về **cách bảo trì `PROJECT_CONTEXT.md`** ("File này PHẢI được cập nhật khi nào") — không user IELTS thật nào hỏi Navita câu này. Nguyên nhân: bước Kaggle không lọc mục dev-facing như bước template đã làm ở trên, 2 bước lệch nhau. Sửa: `ai/training/extract_kaggle_chunks.py` (4 test) lọc lại theo **đúng danh sách mục đã chọn ở `generate_template_data.py`** (dùng chung, không định nghĩa lần 2 để khỏi lệch tiếp) — còn **37/139 đoạn** (bỏ hết `document.txt` + mọi mục dev-facing của `PROJECT_CONTEXT.md`). Notebook build lại, đã tự kiểm lại: 37/37 đoạn parse đúng, đúng 2 nguồn `PROJECT_CONTEXT.md`/`USER_GUIDE.md`, không lỗi cú pháp, chữ trong notebook cũng cập nhật số liệu (không còn hardcode "139"). **Ảnh hưởng cần báo user**: tổng số cặp dữ liệu giờ khả năng thấp hơn ước tính ~200–300 ban đầu (37 đoạn thay vì 139) — cần hỏi lại user có cần bổ sung nguồn khác không, hay ~65–100 cặp là đủ cho mục tiêu "chỉnh giọng văn" (không cần nhồi kiến thức).
- [x] Thử chạy song song 2 GPU (theo yêu cầu user) — **user chạy thật trên Kaggle, gặp lỗi thật**: `torch.OutOfMemoryError` trên GPU thứ 2 khi nạp bản model độc lập thứ 2. Nguyên nhân thật (đọc từ log lỗi user gửi): Qwen3.5-9B kèm sẵn 1 vision encoder (26 khối) + module multi-token-prediction — không dùng tới (bị đánh dấu `UNEXPECTED` khi nạp qua `AutoModelForCausalLM`), nhưng vẫn bị đọc vào bộ nhớ GPU trước khi bị bỏ, khiến **1 bản model đã chiếm ~14.33/14.56GB của cả 1 GPU T4** — không đủ chỗ cho bản thứ 2 trên GPU khác. Đã hỏi lại user 2 hướng xử lý (bỏ 2 GPU giữ 9B, hoặc đổi sang 4B để giữ song song thật) — user chọn **bỏ song song, quay về 1 GPU, giữ 9B** (37 đoạn, workload nhỏ nên không cần tăng tốc gấp đôi).
- [x] Sửa lại notebook: chỉ nạp **1 model trên 1 GPU** (`cuda:0`, hoặc CPU nếu không có GPU). Vẫn giữ cấu trúc `ThreadPoolExecutor`/`run_parallel()` (giờ `max_workers=1`, tương đương vòng lặp tuần tự) — không viết lại, để dễ bật lại song song sau này nếu đổi model/máy. Đã tự kiểm lại: 37/37 đoạn parse đúng, đúng 2 nguồn, không lỗi cú pháp, mô phỏng chạy thử logic (10 đoạn giả, 1 lỗi cố ý) — không sót/trùng đoạn nào, lỗi bị bắt riêng lẻ đúng như trước.
- [x] **Lỗi thật thứ 3 (do user chạy thật phát hiện, ở đoạn 21/37, sau khi đã sửa 2 lỗi trên)**: `TypeError: 'str' object is not a mapping` khi tổng hợp kết quả — làm sập toàn bộ run giữa chừng, mất luôn dữ liệu 20 đoạn đã xử lý trước đó (chỉ mất vì crash, không phải vì đoạn đó thật sự lỗi). Nguyên nhân: `extract_json_array()` chỉ `json.loads()` rồi tin luôn, không kiểm tra hình dạng — khi model trả về `["câu hỏi 1", "câu hỏi 2"]` (mảng chuỗi) thay vì `[{"question":...,"answer":...}]` (mảng object) như yêu cầu, nó vẫn "parse thành công" nhưng `**pair` ở bước tổng hợp sau đó sập vì `pair` là string, không phải dict.
- [x] **Sửa tận gốc + đổi cách làm**: tách hẳn phần logic (`build_prompt`, `extract_json_array` có validate hình dạng, `process_one`, `run_parallel`) ra module Python thật `ai/training/kaggle_generation.py`, viết **15 test pytest** cho nó (bao gồm đúng test tái hiện lỗi này: mảng chuỗi phải bị từ chối). `build_kaggle_notebook.py` giờ **nhúng nguyên văn file đã test này** vào notebook thay vì gõ tay lại logic tương đương — lý do đổi cách làm: cả 2 lỗi thứ 2 và thứ 3 đều xuất phát từ việc gõ tay logic trực tiếp vào notebook mà không qua test, đúng loại lỗi 1 bộ test đơn giản trên object Python thuần đã bắt được ngay. Đã xác nhận bằng chính code embedded trong notebook (không phải giả lập riêng): mô phỏng 1 chunk trả lời đúng hình dạng lỗi thật (`["a","b"]`) — bị bắt lỗi riêng lẻ, không sập batch, 7/8 chunk còn lại vẫn xử lý bình thường.
- [x] User chạy thành công trên Kaggle: 32/37 đoạn thành công (5 lỗi bị bắt riêng lẻ đúng thiết kế — không sập batch), `generated_qa.jsonl` (64 cặp) + `generation_errors.json` đưa lại cho tôi
- [x] **Phát hiện thật khi xem dữ liệu**: 30/64 cặp (47%) dồn hết vào riêng mục "9. Hệ thống theme" — mục này dài/kỹ thuật nên bị chunk thành nhiều đoạn hơn hẳn các mục khác, kéo theo sinh ra nhiều cặp hơn hẳn dù không tương xứng mức độ quan trọng với người dùng thật; vài câu còn quá thiên kỹ thuật (VD "vì sao dùng quy tắc đếm màu sáng thay vì độ sáng trung bình"). Sửa bằng `ai/training/build_final_dataset.py` (11 test): bỏ trùng lặp gần giống (`difflib`, ngưỡng 0.85) + giới hạn tối đa 4 cặp/mục — thuần túy CHỌN LỌC trong số cặp model đã sinh, không viết lại/sửa nội dung nào (giữ đúng nguyên tắc không để Claude "sáng tác" dữ liệu train). Kết quả: 64 → 32 cặp model-generated, cân bằng lại đúng như 12 mục đã chọn (tối đa 4/mục).
- [x] Ghép với 26 cặp template → **58 cặp tổng cộng** (`ai/data/processed/train_final.jsonl`, gitignore)
- [x] Sinh file duyệt cho user (`ai/training/make_review_file.py` → `ai/data/processed/review_sample.md`) — **hiện TOÀN BỘ 58 cặp** thay vì mẫu 30 ngẫu nhiên như kế hoạch gốc, vì tổng số giờ đủ nhỏ để duyệt hết thay vì chỉ xem 1 phần; đổi từ "hiện trong chat" sang file Markdown vì có vài câu template rất dài (nguyên văn đoạn tài liệu), dồn hết vào 1 tin nhắn chat sẽ khó đọc
- [x] User duyệt file `review_sample.md` — trả lời "Đồng ý hết" (2026-09-19), giữ nguyên cả 58 cặp, không loại cặp nào
- [x] Lọc dữ liệu (trùng lặp, sai, quá ngắn/dài) — đã làm ở `build_final_dataset.py` (bước trên, trước khi sinh file duyệt): bỏ trùng lặp gần giống + giới hạn 4 cặp/mục
- [x] User duyệt trước khi train — lệch so với kế hoạch gốc ("~30 mẫu ngẫu nhiên, hiện trong chat"): duyệt **toàn bộ 58 cặp** qua file `review_sample.md` thay vì mẫu ngẫu nhiên (lý do đã ghi ở dòng trên), user "Đồng ý hết"
- [x] Viết notebook Kaggle fine-tune LoRA — `ai/training/build_lora_training_notebook.py` sinh `ai/kaggle/train_lora_kaggle.ipynb` (gitignore, tái tạo bằng script). Dùng **Unsloth + TRL SFTTrainer** (đã tra cứu API thật trước khi viết, không đoán): LoRA 16-bit thường trên Qwen3.5-4B (không QLoRA, đúng khuyến cáo mục 5), `assistant_only_loss=True` (Qwen3.5 nằm trong danh sách model TRL hỗ trợ sẵn cho việc này), `max_length=6144` (đo thật bằng tokenizer thật trên cả 58 mẫu: dài nhất 4656 token). Phần thuần logic (nạp/kiểm/tách dữ liệu) tách thành `ai/training/lora_training_data.py` + **16 test pytest**, nhúng nguyên văn vào notebook (không gõ tay lại — đúng bài học từ 2 lỗi Kaggle trước). Đã tự kiểm offline (không cần GPU): cell ghi file dữ liệu round-trip đúng 100% với `train_final.jsonl` gốc, cell nạp+tách chạy đúng (58 tổng → 52 train + 6 giữ lại kiểm tra). Cell nạp model/train/sinh câu trả lời (cần GPU) **chưa tự kiểm được**, đợi user chạy thật trên Kaggle.
- [x] **Lỗi thật thứ 4 (user chạy thật trên Kaggle, phát hiện ngay ở bước tạo `SFTConfig`)**: `ValueError: Your setup doesn't support bf16/gpu` — do notebook hardcode `bf16=True`. Nguyên nhân: GPU T4 miễn phí của Kaggle là kiến trúc Turing, chỉ GPU Ampere trở lên (A100, RTX 30xx+...) mới có nhân bf16 phần cứng. Sửa: dùng `torch.cuda.is_bf16_supported()` để tự phát hiện đúng lúc chạy, `bf16=bf16_supported, fp16=not bf16_supported` — không hardcode 1 trong 2 nữa (tương thích luôn nếu Kaggle đổi loại GPU). Đã tự kiểm lại offline: cell ghi/nạp/tách dữ liệu vẫn đúng 100% sau khi sửa.
- [x] **Lỗi thật thứ 5 (user chạy thật trên Kaggle sau khi sửa lỗi 4, phát hiện ngay ở bước tạo `SFTTrainer`)**: `ValueError: Assistant-only loss is not yet supported for vision-language models`. Nguyên nhân thật (đọc thẳng mã nguồn TRL v1.13.0 để xác nhận, không đoán): Unsloth nạp `processing_class` của Qwen3.5-4B là 1 `ProcessorMixin` (bộ xử lý đa phương thức — model này về gốc hỗ trợ cả ảnh, dù tác vụ của mình chỉ dùng text), nên TRL coi model là "vision-language model" và **chặn cứng `assistant_only_loss`** cho trường hợp này (không liên quan gì đến dữ liệu train của mình, chỉ do cách phân loại của TRL). Sửa: đổi dữ liệu từ dạng `messages` sang dạng `prompt`/`completion` (hàm mới `to_prompt_completion()`, 3 test) + dùng `completion_only_loss=True` thay cho `assistant_only_loss=True` — cùng hiệu quả (chỉ tính loss trên câu trả lời) nhưng qua cơ chế TRL **không** chặn với model bị coi là đa phương thức. Đã tự kiểm lại offline: 19 test `lora_training_data.py` xanh, dữ liệu sau khi tách vẫn đúng cấu trúc `{"prompt": [system, user], "completion": [assistant]}` (85/85 test toàn bộ `ai/` xanh).
- [x] **Lỗi thật thứ 6 (user chạy thật trên Kaggle sau khi sửa lỗi 5, phát hiện ở bước tokenize dữ liệu train, tức bước ngay sau khi tạo `SFTTrainer` thành công)**: `TypeError: string indices must be integers, not 'str'` sâu trong `processing_class.apply_chat_template()` (dòng lỗi: `content["type"]`). Nguyên nhân thật: `content` của mỗi message trong dữ liệu của mình vẫn là 1 chuỗi thường (`"content": "câu hỏi..."`), nhưng `Processor` đa phương thức của Qwen3.5-4B (đã phát hiện ở lỗi 5) đòi `content` phải là **1 danh sách khối nội dung** (`[{"type": "text", "text": "..."}]`, đúng định dạng chuẩn của mọi model đa phương thức họ Qwen-VL, đã tra cứu docs Qwen-VL để xác nhận) — lặp 1 chuỗi thường ra từng ký tự rồi index bằng `["type"]` chính là lỗi này. Sửa: `to_prompt_completion()` giờ bọc content mỗi message thành khối `[{"type":"text","text":...}]` qua hàm mới `_as_content_blocks()`; áp dụng luôn cho cell smoke-test (mục 6) vì cell đó gọi `apply_chat_template` y hệt cách này. Đã tự kiểm lại offline: 20 test `lora_training_data.py` xanh (thêm 1 test regression đúng lỗi này), 86/86 test toàn bộ `ai/` xanh, dữ liệu sau khi tách đúng cấu trúc content-block.
- [x] **Lỗi thật thứ 7 (user chạy thật trên Kaggle sau khi sửa lỗi 6 — tokenize xong, train thật sự bắt đầu chạy, sập giữa chừng trong backward pass)**: `RuntimeError: expected mat1 and mat2 to have the same dtype, but got: c10::BFloat16 != c10::Half`, sập ở layer `Qwen3_5GatedDeltaNet` (1 kiểu layer "linear attention" mới của Qwen3.5, xen giữa các layer attention thường — không phải lỗi ở dữ liệu/cấu hình train của mình). Tra cứu tìm ra đây là **lỗi công khai đã có người báo trùng khớp**: [unslothai/unsloth #4970](https://github.com/unslothai/unsloth/issues/4970) — trên GPU không có bf16 phần cứng như T4, Unsloth nạp model ở bf16 (đúng gốc checkpoint) rồi hạ 1 phần trọng số xuống fp16, nhưng activation truyền qua vẫn còn bf16 ở layer `GatedDeltaNet` — lệch dtype ngay tại phép nhân ma trận. Unsloth đã có fix chính thức (PR [unsloth-zoo #978](https://github.com/unslothai/unsloth-zoo/pull/978)): set biến môi trường `UNSLOTH_FORCE_FLOAT32=1` **trước khi** `import unsloth` để bật các lớp bọc dtype-safety cho đúng layer bị lỗi — không cần tự viết fix, chỉ cần bật đúng flag có sẵn. Đã thêm vào notebook + nâng cấp luôn `unsloth_zoo` ở bước cài đặt (đảm bảo có bản đã vá). Không cần sửa `lora_training_data.py` (lỗi thuần ở tầng model loading, không liên quan dữ liệu) — 86/86 test toàn bộ `ai/` vẫn xanh.
- [x] **Sửa lỗi thứ 7 KHÔNG hiệu quả — nhận định sai, đổi hướng hẳn**: user chạy lại, `UNSLOTH_FORCE_FLOAT32=1` **không có tác dụng gì**, sập lại y hệt lỗi cũ (cùng traceback, cùng dòng `Qwen3_5GatedDeltaNet`). Tra lại kỹ hơn: pull request chứa fix đó ([unsloth-zoo #978](https://github.com/unslothai/unsloth-zoo/pull/978)) **vẫn đang mở, chưa merge** — bản Unsloth cài qua `pip install -U` không hề có code fix này. **Lỗi nhận định của tôi ở bước trước**: thấy có pull request đề xuất fix rồi coi luôn là "đã có fix chính thức", không kiểm tra trạng thái merge/phát hành trước khi báo cho user — cần rút kinh nghiệm: 1 PR tồn tại ≠ 1 fix đã lên bản phát hành. Vì đây là bug thật của chính Unsloth, chưa có ai sửa xong, không rõ bao giờ mới có bản vá — quyết định **bỏ hẳn Unsloth cho bước train này**, chuyển sang `transformers` + `peft` + `trl` thuần (không hand-roll gì thêm, vẫn dùng thư viện chuẩn như chủ trương ban đầu). Viết lại `build_lora_training_notebook.py`: model nạp bf16 gốc (không ép kiểu) qua `AutoModelForCausalLM` thường (không qua bản Qwen3.5 tự biên dịch lại của Unsloth nên tránh hẳn lỗi 7), tokenizer thường qua `AutoTokenizer` (không phải `Processor` nên tránh luôn lỗi 5 và 6 — quay lại được dạng dữ liệu `messages` + `assistant_only_loss=True` ban đầu, đơn giản hơn, bỏ hẳn `to_prompt_completion()`/`_as_content_blocks()` khỏi `lora_training_data.py`, còn 16 test); `SFTConfig(bf16=False, fp16=False)` — không bật autocast của Trainer nữa nên tránh luôn lỗi 4 theo cách khác (model tự chạy đúng dtype đã nạp, không cần dò `torch.cuda.is_bf16_supported()`); `target_modules="all-linear"` thay vì liệt kê tên cố định — phát hiện thêm 1 vấn đề (không phải lỗi sập, nhưng đáng sửa luôn): layer `GatedDeltaNet` dùng tên proj khác hẳn (`in_proj_qkv`, thấy thẳng trong traceback lỗi 7) nên danh sách tên cố định trước đó **bỏ sót hoàn toàn các layer này khỏi LoRA** — `"all-linear"` tự phủ đúng mọi layer Linear (trừ lm_head, đúng docs PEFT). Đánh đổi: mất tối ưu bộ nhớ/tốc độ riêng của Unsloth, chưa biết có vừa VRAM T4 không (đợi chạy thật). Đã tự kiểm lại offline: 82/82 test toàn bộ `ai/` xanh (giảm 4 test do bỏ `to_prompt_completion`/`_as_content_blocks`), dữ liệu ghi/nạp/tách vẫn đúng 100%.
- [x] **Lỗi thật thứ 8 (user chạy bản bỏ Unsloth — tải model 9.32GB xong, nạp weight xong, sập ngay ở bước gắn LoRA)**: `ImportError: Found an incompatible version of torchao. Found version 0.10.0, but only versions above 0.16.0 are supported`. Khác hẳn 7 lỗi trước — **không liên quan model/GPU/dữ liệu**, thuần túy môi trường: gói `torchao` có sẵn trên máy ảo Kaggle (0.10.0) cũ hơn mức tối thiểu mà bản `peft` mới nhất (vừa nâng cấp qua `-U`) yêu cầu (0.16.0) cho 1 dispatcher nội bộ (kiểm tra layer có cần xử lý kiểu lượng tử hoá `torchao` không — không áp dụng cho mình vì train bf16 thường, nhưng `peft` vẫn kiểm tra qua nên vẫn sập). Sửa: thêm `torchao` vào danh sách nâng cấp ở bước cài đặt (`pip install -U ... torchao`). Đã tự kiểm lại: 82/82 test toàn bộ `ai/` vẫn xanh (không đụng logic nào, chỉ thêm 1 gói vào lệnh cài đặt).
- [x] **Lỗi thật thứ 9 (user chạy bản vá torchao — tokenize xong, train thật sự bắt đầu, sập ngay bước đầu)**: `RuntimeError: Expected all tensors to be on the same device, but got index is on cuda:1, different from other tensors on cuda:0`. Nguyên nhân: Kaggle cấp 2 GPU T4; dù đã cố định model vào `cuda:0` lúc nạp, `transformers.Trainer` tự phát hiện có 2 GPU và tự động bọc model bằng `torch.nn.DataParallel` để "tận dụng" cả 2 — dàn 1 bản model sang `cuda:1` trong khi batch dữ liệu vẫn ở `cuda:0`, lệch device ngay ở bước tra bảng embedding đầu tiên. Không liên quan model/dữ liệu, thuần túy do máy có 2 GPU mà mình chỉ định dùng 1. Sửa: `os.environ["CUDA_VISIBLE_DEVICES"] = "0"` đặt trước `import torch` — chặn hẳn GPU thứ 2 khỏi tầm nhìn của tiến trình nên `Trainer` không còn thấy 2 GPU để tự động dùng `DataParallel` nữa. Đã tự kiểm lại: 82/82 test toàn bộ `ai/` vẫn xanh (không đụng logic dữ liệu).
- [x] **Lỗi thật thứ 10 (user chạy bản chặn 1 GPU — train thật sự chạy, tới step 2/39 mới sập)**: `OutOfMemoryError: CUDA out of memory` (~14.01/14.56GB đã dùng). Nguyên nhân: bỏ Unsloth cũng mất luôn phần Unsloth tự tối ưu bộ nhớ; model 4B ở bf16 nguyên bản chiếm ~8GB chỉ riêng trọng số, cộng thêm hoạt động (activation) của 1 câu dài là tràn. Sửa 2 lớp: (1) nén base model xuống **8-bit** qua `BitsAndBytesConfig(load_in_8bit=True)` + `prepare_model_for_kbit_training()` — khác hẳn 4-bit/QLoRA (thứ đang tránh cho Qwen3.5), 8-bit là cách được cộng đồng công nhận an toàn để train, giảm ~1 nửa bộ nhớ phần trọng số; (2) hạ `max_length` từ 6144 xuống 2048 làm biên an toàn thứ 2 — đổi lấy việc câu trả lời dài ngoại lệ duy nhất (4656 token, về bảng màu giao diện) bị cắt bớt phần cuối, còn 57/58 mẫu khác (95% dưới 1521 token) vẫn nguyên vẹn. Đã tự kiểm lại offline: 82/82 test toàn bộ `ai/` xanh, dữ liệu ghi/nạp/tách vẫn đúng 100%.
- [x] **Cải thiện nhỏ (không phải lỗi sập)**: bản chạy 8-bit in ra rất nhiều dòng cảnh báo vô hại `MatMul8bitLt: inputs will be cast...` (1 dòng/layer/bước train) làm ngợp output, khiến trình duyệt/notebook bị lag dù train vẫn chạy đúng tiến trình. Đã thêm `warnings.filterwarnings(...)` để tắt riêng dòng này ở lần chạy sau. Đã tự kiểm lại: 82/82 test toàn bộ `ai/` vẫn xanh.
- [x] User chạy lại notebook trên Kaggle — **train thành công** (39/39 bước, loss giảm đều 2.71 → 1.36), tải `lora_adapter.zip` về đặt vào `ai/kaggle/`. Giải nén vào `ai/training/output/` (gitignore, thêm `/ai/training/output/` vào `.gitignore`). Kiểm `adapter_config.json`: `target_modules` có đủ cả tên chuẩn (`q_proj`...) lẫn tên riêng của `GatedDeltaNet` (`in_proj_qkv`, `in_proj_a/b/z`, `out_proj`) — xác nhận `target_modules="all-linear"` đã phủ đúng cả 2 loại layer như tính toán.
- [x] **Đổi hướng giữa chừng**: kế hoạch gốc dùng `Modelfile` + `ADAPTER` để gắn rời adapter vào model gốc — chạy `ollama create` thì Ollama báo `Error: LoRA adapters are no longer supported` (Ollama bỏ tính năng này từ bản 0.34.1, đã tra cứu xác nhận, không phải lỗi phía mình). Cách thay thế hiện tại của Ollama: **gộp cứng** adapter vào model gốc (`peft.merge_and_unload()`) thành model độc lập, tự chạy toàn bộ cục bộ (không cần Kaggle nữa, không cần GPU): tải lại `Qwen/Qwen3.5-4B` gốc (~9.3GB) về máy user, nạp qua `transformers` + `peft`, gộp, lưu ra `ai/training/output/merged/` (đã xoá sau khi convert xong, tái tạo được bằng cách chạy lại).
- [x] **Lỗi thật khi convert GGUF (bản adapter rời, trước khi đổi sang gộp)**: `convert_lora_to_gguf.py` (clone từ chính repo `llama.cpp`, đã xác nhận có hỗ trợ đầy đủ kiến trúc Qwen3.5/`GatedDeltaNet` — class `Qwen3_5TextModel`, thêm đúng hôm qua) sập ở tensor `out_proj` với `NotImplementedError` khi cố sắp xếp lại "V-heads" trên 1 delta LoRA (dạng phân rã A/B hạng thấp) — thư viện chỉ hỗ trợ phép này trên tensor thường (dùng khi convert cả model gốc), chưa hỗ trợ cho dạng factored A/B của LoRA. Việc chuyển sang gộp cứng (mục trên) né được lỗi này hoàn toàn vì gộp xong không còn là LoRA delta nữa, chỉ còn trọng số thường.
- [x] Convert model đã gộp sang GGUF (`convert_hf_to_gguf.py`, nén `q8_0` để vừa máy có GPU 4GB VRAM của user — ~4.5GB, so với 3.4GB bản Q4 gốc) — **2 lỗi thật khi Ollama nạp model** (không phải lỗi convert, phát hiện lúc `ollama run`/gọi API thật): (1) `tensor 'blk.32.attn_norm.weight' not found` — model gốc có thêm 1 "layer" MTP (multi-token-prediction, tính năng không dùng tới, đã biết từ Milestone 7 khi tải model 9B) mà `AutoModelForCausalLM` không nạp vào (weight "unexpected", bị bỏ), nhưng file cấu hình GGUF vẫn khai 33 layer thay vì 32 layer thật — tự viết script sửa trực tiếp metadata GGUF (`qwen35.block_count` 33→32, cắt mảng `qwen35.attention.recurrent_layers` từ 33 xuống 32 phần tử) bằng chính thư viện `gguf` của llama.cpp; (2) sau khi sửa (1), lỗi mới `tensor 'blk.31.nextn.eh_proj.weight' not found` — cùng nguyên nhân MTP, còn sót 1 field khai `qwen35.nextn_predict_layers=1`, sửa về `0` bằng script `gguf_set_metadata.py` có sẵn của llama.cpp. Sau 2 lần sửa: `ollama create` + gọi API thật **thành công**, model `qwen3.5-4b-project` (4.5GB) chạy được.
- [x] **Hư fake-alarm, không phải lỗi thật**: lần gọi thử API đầu tiên qua `curl -d '...'` (nhúng thẳng tiếng Việt vào dòng lệnh) ra chữ bị vỡ (`�`, `?`) — tưởng model/GGUF lỗi encoding, nhưng test lại bằng cách ghi JSON ra file rồi `curl --data-binary @file` (tránh encoding dòng lệnh shell) thì **cả model gốc lẫn model fine-tune đều ra tiếng Việt sạch** — xác nhận lỗi chỉ do cách gọi `curl` qua Bash trên Windows, không phải lỗi thật ở model.
- [x] Test nhanh 1 câu hỏi thật (đúng câu ví dụ #17 trong dữ liệu — câu về bảng màu giao diện): model fine-tune trả lời khác hẳn model gốc (gốc chung chung/nhận không chắc; bản fine-tune trả lời cụ thể, giọng kỹ thuật đúng phong cách dự án) — xác nhận fine-tune có tác dụng thật. Lưu ý: câu #17 là câu trả lời **bị cắt bớt lúc train** (dài 4656 token, vượt `max_length=2048` đã hạ ở lỗi thứ 10) nên riêng câu này có thể chưa đủ chi tiết như tài liệu gốc — các câu khác (57/58, dưới 1521 token) không bị ảnh hưởng.
- [x] Dọn file trung gian không cần giữ: `ai/training/output/merged/` (8.4GB, bản model gộp đầy đủ trước khi nén — tái tạo được), `gguf_export/` (bản adapter đã lọc bớt `out_proj`, chỉ dùng cho hướng đã bỏ), `adapter.gguf` (adapter rời dạng GGUF, không dùng được nữa vì Ollama bỏ ADAPTER). Còn giữ: `adapter_model.safetensors`+`adapter_config.json` (adapter gốc, hồ sơ thật đã train), `qwen3.5-4b-project.gguf` (model cuối đã gộp+nén, đang dùng), `Modelfile`.
- [x] **Đánh giá trước khi "chốt" (mục 12.5 kế hoạch)**: `ai/training/output/eval_questions.py` hỏi song song 10 câu (6 held-out thật từ `split_train_eval` — chưa từng vào tập train — + 4 câu hoàn toàn mới) cho cả `qwen3.5:4b` và `qwen3.5-4b-project`, chế độ Flash, cùng system prompt thật, qua `httpx` trực tiếp (không qua `curl` dòng lệnh, tránh lại lỗi encoding đã gặp). Kết quả lưu `ai/training/output/eval_results.json` + báo cáo so sánh đầy đủ dạng artifact cho user. **Phát hiện chính** (dữ liệu thật, không suy đoán):
  - Fine-tune nhanh hơn **~2.9 lần** trung bình (21.8s vs 63.0s) và ổn định hơn nhiều (19–25s so với 21–97s).
  - Giọng văn đúng phong cách dự án rõ rệt khi trả lời đúng — ngắn gọn, cụ thể, không rào trước đón sau như bản gốc.
  - Kiến thức IELTS chung (2 câu mới, không liên quan riêng app) — **cả 2 model đều đúng**, fine-tune không bị suy giảm.
  - **Nhưng 4/6 câu held-out có chi tiết/công thức sai** (nhầm tên biến theme, công thức làm tròn Band thiếu mốc, công thức Band dự đoán sai hoàn toàn dù câu này KHÔNG bị cắt ngắn lúc train, chiều tác động của xác suất quay bị đảo ngược) — 52 mẫu/3 epoch/LoRA r=16 đủ học giọng văn nhưng chưa đủ nhớ chính xác chi tiết kỹ thuật.
  - **Phát hiện nghiêm trọng nhất**: câu kiểm tra tính trung thực ("app có nhắc lịch giờ cố định không" — tính năng không có thật) — bản gốc trả lời đúng "không chắc", bản fine-tune **khẳng định chắc nịch tính năng đó tồn tại** (bịa hoàn toàn). Dữ liệu train 58 mẫu toàn là câu hỏi có-đáp-án-thật, không có mẫu nào dạy "không chắc" — fine-tune vô tình làm giảm khả năng thừa nhận không biết, đúng điều system prompt yêu cầu tránh.
  - **Đề xuất đưa ra cho user**: chưa nên đổi `CHAT_MODEL` sang bản fine-tune làm mặc định ngay — cần thêm dữ liệu train dạng "không có đáp án chắc chắn" trước khi cân nhắc lại, hoặc tiếp tục dùng bản gốc.
- [ ] User xem báo cáo đánh giá, quyết định: (a) đổi `CHAT_MODEL` sang `qwen3.5-4b-project` bất chấp rủi ro đã nêu, (b) giữ nguyên `qwen3.5:4b`, hay (c) làm thêm 1 vòng dữ liệu train (thêm mẫu "không chắc") rồi train lại trước khi quyết định
- [ ] Review chốt Milestone 7 (1 lượt Supervisor review gated theo CLAUDE.md, sau khi mục trên xong)

## Milestone 8 — Chấm Writing/Speaking theo IELTS (chưa bắt đầu)

- [ ] Tải faster-whisper (small/medium) vào `ai/models/`
- [ ] Khung chấm Writing: rubric 4 tiêu chí (Task Achievement/Response, Coherence & Cohesion, Lexical Resource, Grammatical Range & Accuracy)
- [ ] Khung chấm Speaking: thu âm → faster-whisper (STT) → chấm 4 tiêu chí (Fluency & Coherence, Lexical Resource, Grammatical Range & Accuracy, Pronunciation ở mức tương đối)
- [ ] Trang chấm riêng (không ghi DB, không đụng công thức Band — hiển thị tại chỗ)
- [ ] Review chốt Milestone 8
