DOCUMENT.TXT — Deferred Features & Future Updates Log
======================================================
This is a running, append-only log. When a deferred item is actually built, move it
to the "DONE" section at the bottom with the date and which milestone did it — don't
just delete it. When a new "do this later" idea comes up mid-task, add it here instead
of letting it get lost.

Created: 2026-09-17 (Phase 0)


PENDING
-------

1. Authentication / multi-user support
   Why deferred: app is local-only, single implicit user for now.
   Trigger to build: if the app needs to be used from more than one device, or shared.

2. Cloud deployment (Vercel / VPS / etc.)
   Why deferred: local-only for now (`npm run dev`).
   Trigger to build: same as #1 — needed once multi-device access matters.
   Depends on: #1 (auth) should probably land first if deploying publicly.

3. Adjustable baseRatio via UI for Writing (Task1/Task2) and Listening (Block A/B)
   Why deferred: original spec only called out Speaking/Reading Block A vs B as
   needing a user-adjustable ratio (60/40 default). Writing and Listening use a fixed
   50/50 prior for now. The schema already supports this (SkillPart.baseRatio exists
   for every part) — this is a UI-only addition, no schema/migration needed.

4. Band Prediction v3 — OLS Linear Regression per skill
   Why deferred: v1's flat mean of the last 30 Cambridge test results per skill has
   already been replaced (Milestone 5 mở rộng, done) by v2 — an EWMA
   (src/lib/engine/ewma.ts) plus a practice-accuracy component for Reading/Listening,
   see PROJECT_CONTEXT.md section 5.4. This item is the next step beyond that: once
   enough historical CambridgeTestResult rows exist per skill, replace (or blend)
   `cambridgeEwma_skill` in bandPrediction.ts with an independent ordinary-least-squares
   regression (band ~ testDate or band ~ testIndex) per skill, and use the projected
   trend value instead of (or blended with) the EWMA. The frequency/accuracy-nudge
   layers on top stay unchanged. See PROJECT_CONTEXT.md section 5.6.
   Trigger to build: enough data — suggest revisiting once a skill has 15-20+ logged
   Cambridge results, since a regression on very few points is unreliable.

5. Weekly-minimum-appearance safety net at the Part/Block level
   Why deferred: the "must appear >= 1x/week" hard override (Formula 2) currently
   only applies to the 4-skill pool, per explicit scope in the original spec. Parts/
   blocks (e.g. Speaking Block B) rely purely on the inverse-count weighting with no
   hard floor, so in theory a rarely-drawn block could go a long time without being
   forced. Consider adding the same kind of override at the part level if that turns
   out to be a problem in practice.

6. Count soft-reset activation / tuning
   Why deferred: PROJECT_CONTEXT.md section 5.2.1 specifies a safeguard (rescale all
   counts in a pool down when max count crosses `count_soft_reset_threshold`, default
   50) to keep numbers bounded over the app's long-term lifetime. This needs to be
   implemented in Milestone 1 as designed, but the threshold value (50) is a guess —
   revisit once real usage data shows how fast counts actually grow (e.g. if the user
   rolls twice a day, 50 rolls is ~25 days per pool item, which may be too aggressive
   or too lax — tune after observing real numbers).

7. Config UI for engine constants (currently only via API/DB)
   Why deferred: not explicitly requested; `GET/PATCH /api/config` exists per
   PROJECT_CONTEXT.md section 6, but no UI screen was scoped for editing
   decay_exponent, weekly_threshold_days, frequency_adjustment_factor/cap, or
   count_soft_reset_threshold directly. The Speaking/Reading baseRatio adjustment
   (explicitly requested) is the only config exposed in the UI for Milestone 2.
   Trigger to build: if the user wants to tune engine constants without editing the
   DB/API directly.

8. [IN PROGRESS, not deferred — tracked here only until it graduates to its own
   DONE entry] Milestone 6/7/8 — Chatbot AI. Full scope/decisions log lives in
   AI_CHATBOT_PLAN.md (repo root) — read that first, it is the source of truth,
   not this bullet. Summary: user wants a project-specific chatbot (Q&A about
   the app + general IELTS tutoring + advice from the user's own DB data), plus
   a separate Writing/Speaking scorer against official IELTS band criteria.
   Repo restructured Milestone 6 Step 1: whole app moved into web/, new ai/
   module added (models/, data/raw+processed/, training/, server/), new AI/ML
   role + `.claude/agents/ai-engineer.md` added to CLAUDE.md. Model choice:
   Qwen3.5-4B-Instruct (GGUF 4-bit, local inference) + Qwen3.5-9B (Kaggle-only,
   synthetic training-data generation) + faster-whisper (Milestone 8 STT) —
   all open models, deliberately NOT Claude: checked anthropic.com/legal/aup
   directly, which prohibits using Claude input/output to train any AI model
   without prior authorization, no carve-out found for non-competing/personal
   use — this overturned an earlier in-conversation choice by the user before
   any data was actually generated. See AI_CHATBOT_PLAN.md for the full
   reasoning trail and all other decisions (data-gen method, milestone split,
   language behavior, why Writing/Speaking scoring doesn't touch the DB yet).


DONE
----

[2026-09-17] Milestone 1, Step 1: Scaffold + DB layer.
  - Scaffolded Next.js 16.3.5 / React 19.2.8 (TypeScript, Tailwind v4, App Router)
    via create-next-app (had to scaffold into a temp dir with a lowercase name and
    merge in, since npm rejects package names with uppercase letters and the
    project folder is named "EL").
  - ARCHITECTURE CHANGE: PROJECT_CONTEXT.md originally specified Prisma ORM.
    While installing it, discovered Prisma's latest version (8.0.0-rc.15) has
    pivoted into a cloud platform product and its guided ORM setup no longer
    supports SQLite (only postgres/mongodb), the classic `prisma generate` /
    `migrate dev` commands are gone entirely, and its local dev stack doesn't
    support Windows. Switched to Drizzle ORM + better-sqlite3 instead (user
    confirmed this direction). PROJECT_CONTEXT.md section 2.1 has the full
    writeup. Schema/data model itself is unchanged, only the tooling.
  - Created `src/lib/db/schema.ts` (7 tables), `client.ts` (better-sqlite3 +
    drizzle singleton), `seed.ts` (4 skills, 8 parts, 5 Config defaults).
  - Had to approve native install scripts (better-sqlite3, esbuild, unrs-resolver)
    via `npm approve-scripts` — npm 11.17 blocks install scripts by default now.
  - git initialized locally; Phase 0 docs committed as the root commit.

[2026-09-17] Milestone 1, Steps 2-3: Math engine + API routes. MILESTONE 1 DONE.
  - Step 2: implemented all 3 formulas as pure functions in src/lib/engine/
    (weightedRandom.ts, weeklyConstraint.ts, bandPrediction.ts, ieltsRounding.ts,
    countSoftReset.ts). Vitest added, 33 initial tests, grew to 48 after a
    Supervisor review pass added edge-case coverage (floating-point sum-to-1,
    negative/fractional decay exponents, statistical distribution checks, the
    7-day and all-null-first-run boundaries, no-clamp ieltsRound behavior).
  - Review pass fixed one real (low-probability) bug: pickWeightedWithoutReplacement
    removed the drawn item by `id` instead of by object reference; harmless for
    real DB-backed usage (unique ids) but fixed defensively.
  - Review pass surfaced a genuine design ambiguity: should the overall predicted
    band average each skill's rounded-to-0.5 value, or the raw pre-rounding
    value? User resolved this as a runtime toggle (Config key
    `overall_prediction_rounding_mode`, default `per_skill_rounded`) rather than
    picking one — see PROJECT_CONTEXT.md section 5.4. Milestone 2 must expose
    this as a UI toggle in the Prediction Dashboard (not yet built).
  - Step 3: wired the engine to Drizzle across 10 API routes (see
    PROJECT_CONTEXT.md section 6). POST /api/roll runs inside one fully
    synchronous db.transaction() — better-sqlite3 requires this, confirmed via
    a throwaway smoke test before writing real code (async transaction
    callbacks throw `Transaction function cannot return a promise`).
  - Review pass found and fixed real input-validation bugs across the mutating
    routes: unhandled 500s on malformed/null JSON bodies (fixed with a shared
    `readJsonObject` helper), `?limit=0` being treated as "no limit" on
    /api/cambridge, and unbounded/garbage limit-offset values crashing or
    silently ignored on /api/history. All fixed, verified live with curl.
  - Every step's fixes were verified with `npm run test` + `npm run typecheck`
    + `npm run lint` before committing; dev.db reset to a clean seeded state
    after each round of manual endpoint testing so no test data lingers.
  - Backend is now feature-complete per PROJECT_CONTEXT.md. Next: Milestone 2
    (Frontend/UI Agent) — Spinner, Journal, Prediction Dashboard + History view.

[2026-09-17] Milestone 2 (Frontend/UI Agent), all 5 steps. MILESTONE 2 DONE.
  - Step 1: app shell + nav (3 pages: Vòng quay /, Nhật ký /journal, Dự đoán
    /prediction — no separate /history page; roll history got folded into
    the Spinner page in Step 2 instead).
  - Step 2: Spinner page. Discovered mid-step that this project's installed
    eslint-plugin-react-hooks (v7, React-Compiler-based) flags ANY setState
    reachable from a useEffect as an error, even async-after-await — so
    initial data fetching moved to Server Components (page.tsx) passing
    props down, with client-side fetch only inside event handlers from then
    on. This became the standing pattern for every page (no useEffect
    fetch-on-mount anywhere in the app). Built a dependency-free "slot
    machine" cycling-reveal animation (spinnerAnimation.ts) instead of a
    literal rotating wheel graphic.
  - Step 3: Journal page. Tags are auto-extracted from #word mentions typed
    inline in the note content (tagUtils.ts, unicode-aware for Vietnamese
    tags) rather than a separate tag input field.
  - Step 4: Cambridge test tracking (add/edit/delete, 5-recent + "Xem tất
    cả"/"Thu gọn" toggle) on the /prediction page. Every mutation always
    refetches the list from the server afterward rather than locally
    patching state — deliberate, after a local-sort bug in Step 3's review;
    confirmed as the right tradeoff for this app's scale in Step 4's review.
  - Step 5: Prediction cards, the overall_prediction_rounding_mode toggle
    (resolves the ambiguity logged in Milestone 1), a 30-day
    practice-frequency bar chart (dataviz skill followed: palette validated
    via scripts/validate_palette.js before writing chart code, both light
    and dark), and Speaking/Reading Block A/B ratio sliders. Completes the
    /prediction page started in Step 4.
  - Each step's Supervisor review found and fixed at least one real bug:
    Step 2 (misleading error path when only the post-roll refresh failed,
    plus missing aria-live), Step 3 (invalid/non-antisymmetric sort
    comparator scrambling same-day note order), Step 4 (PATCH could never
    clear an existing Cambridge note — no way to distinguish "key absent"
    from "key null" in the request body), Step 5 (missing aria-label on the
    ratio sliders, plus stale folder-structure doc drift accumulated across
    Steps 2-5, fixed in PROJECT_CONTEXT.md section 3).
  - Found (not fixed, explicitly deferred by the user): an untracked,
    unrelated `src/theme/` directory of Color Hunt palette PNGs predating
    this project, unreferenced by any code. Left alone, not committed, not
    deleted — the user's call if/when they want it gone.
  - Frontend is now feature-complete for the 3 core surfaces. Next:
    Milestone 3 (QA/Testing Agent) — component tests (Vitest+RTL), then
    Playwright e2e tests, per CLAUDE.md's 2-step breakdown.

[2026-09-17] Milestone 3 (QA/Testing Agent), both steps. MILESTONE 3 DONE.
  - Step 1: split vitest.config.mts into "unit" (node) + "component" (jsdom+RTL)
    projects, shared @ alias. Component tests for Spinner (animation mocked —
    its own timing has a dedicated unit test), Journal, CambridgeTracker,
    RoundingModeToggle, PredictionCards, plus a PredictionPageClient
    integration test. Filled a real coverage gap: spinnerAnimation.ts had no
    test despite existing since Milestone 2 Step 2.
  - Step 2: Playwright e2e (roll flow, journal CRUD, Cambridge->prediction).
    Found and fixed a REAL PRODUCTION BUG while setting this up: page.tsx,
    journal/page.tsx, and prediction/page.tsx all read live DB state with no
    dynamic-triggering API, so `next build` silently prerendered them as
    static HTML — a real deploy would have served one frozen snapshot to
    every visitor forever. Fixed with `export const dynamic =
    "force-dynamic"` on all 3. This was invisible under `npm run dev` (which
    never prerenders) and only surfaced because e2e needed a production
    build. Also discovered Next 16's dev-server singleton lock (refuses a 2nd
    `next dev` instance for the same project dir even on a different port),
    so e2e runs against `next build && next start` on a separate port
    instead. DB isolation: setupDb.ts wipes/migrates/seeds a disposable
    ./e2e-test.db, chained via && ahead of build+start in the same shell
    command — an earlier attempt using Playwright's globalSetup hook left the
    test DB empty at runtime because its ordering vs. webServer startup
    wasn't the guarantee it looked like.
  - MID-MILESTONE PROCESS CHANGE: switched from a review-subagent-per-step
    pattern to "Milestone-Gated Review" (one review per whole milestone,
    diff-scoped, max 2 fix-recheck iterations) after the per-step pattern was
    burning 60k-150k+ tokens per small step. CLAUDE.md rewritten accordingly.
    The Step 1 review fixes above still happened per-step (before the
    change); Step 2 + the CLAUDE.md rework itself were self-checked only;
    the milestone-gate review at the end covered both steps' diff at once
    and found 2 more minor issues (a test-quality gap in
    cambridge-prediction.spec.ts, a stale comment).
  - 80/80 unit+component tests, 3/3 e2e, typecheck, lint all green.
    Next: Milestone 4 (Supervisor) — full review pass, USER_GUIDE.md,
    finalize TESTING_GUIDE.md, reconcile document.txt.

[2026-09-17] Theme system built (Milestone 4 prep, ahead of Step 1's review).
  - src/lib/theme.ts: 19 ThemeDefinitions (6 named light->dark ramps + 12
    Color Hunt 4-color palettes + "default") and computeThemeRoles(), which
    derives background/foreground (fixed safe neutral pair, chosen by the
    palette's average luminance), surface/border (palette color closest to
    the background's lightness tier), and primary/primaryForeground (most
    usable saturated/mid-lightness color, synthesized via HSL if none of the
    4 raw colors qualify).
  - src/components/theme/ThemeProvider.tsx (localStorage-backed context,
    lazy useState initializer + a setState-free effect, specifically to
    avoid this project's react-hooks/set-state-in-effect rule — see
    Milestone 2 Step 2's history above) and ThemePicker.tsx (swatch grid),
    wired into a new /settings page + "Cài đặt" nav link.
  - Swept every component off hardcoded bg-white/border-black/bg-foreground
    text-background etc. onto theme-aware bg-surface/border-border/
    bg-primary/text-primary-foreground utilities (globals.css maps 6 CSS
    custom properties through @theme inline). Status colors (delete=red,
    spinner cycling/selected=blue/emerald) intentionally left fixed, not
    themed. Full design rationale in PROJECT_CONTEXT.md section 9.
  - This entry was missing from this log until Milestone 4 Step 1's review
    below caught the gap (the work itself was already reflected in
    FRONTEND_TASKS.md, TEST_TASKS.md, and PROJECT_CONTEXT.md section 9).

[2026-09-17] Milestone 4, Step 1: Supervisor full-app review.
  - Per the user's explicit instruction, did NOT re-review the 3 engine
    formulas, the IELTS rounding rule, or API input validation — all
    reviewed 3x during Milestone 1 and covered by 40+ dedicated unit tests.
    Ran `npm run test` (93 passed) and `npm run test:e2e` (3 passed) first
    to confirm the suite is still green, and stopped there for that layer.
  - Focus: the theme system (never reviewed before) + cross-document
    consistency.
  - REAL BUG FOUND AND FIXED in src/lib/theme.ts: computeThemeRoles picked
    primaryForeground via a flat `relativeLuminance(primary) > 0.5` split.
    The actual WCAG crossover luminance where black vs. white text reach
    equal contrast is ~0.19, not 0.5 — so any primary color with luminance
    in the ~0.19-0.5 band got the LOWER-contrast text color. Verified
    against all 19 real shipped themes: 7 failed WCAG AA (4.5:1) for
    primary/primaryForeground, 3 of those failed even the 3:1 floor (e.g.
    "cold" #2196f3 + white text = 3.12:1; "ch-material-blue" #64b5f7 +
    white = 2.21:1). Fixed by comparing contrast ratio against both
    #111111 and #ffffff and picking whichever is higher — mathematically
    guarantees >= ~4.3:1 in the worst case, and empirically all 19 themes
    now pass 4.5:1 (worst case "dark-winter" at 4.56:1). Added 2 regression
    tests to src/tests/unit/theme.test.ts (a computed-contrast assertion
    across all 19 themes, plus a targeted case reproducing the exact old
    failure). 95/95 unit+component tests green after the fix.
  - Verified, no changes needed: the hardcoded-color sweep is real (no
    remaining bg-white/bg-black/border-black/text-black/bg-gray-*/etc.
    anywhere in src/components or src/app); the surviving `dark:` variants
    are all on the intentionally-fixed status colors (delete=red, spinner
    cycling/selected=blue/emerald), matching PROJECT_CONTEXT.md section 9's
    documented exception; `npx eslint` is clean on ThemeProvider.tsx/
    ThemePicker.tsx/settings/page.tsx; ThemePicker's swatches are native
    <button>s (keyboard-operable by default) with correct aria-pressed and
    an always-visible text theme name (not color-only information); no
    injection vector in ThemePicker's inline style={{...}} — every value
    traces back to the static THEMES array or a pure function over it,
    never to arbitrary/attacker-controlled input (a localStorage-restored
    theme id is validated against the known THEMES list before use).
  - Verified live: started the existing dev server, curled /, /journal,
    /prediction, /settings — all 200, real content, headings intact, nav
    present with bg-surface/border-border classes on every page, no error
    markers. Fetched the compiled CSS chunk directly and confirmed
    .bg-surface/.bg-primary/.border-border/.text-primary-foreground etc.
    compile to real var(--surface)/var(--primary)/... rules, not just
    class names present in markup.
  - Cross-document drift found and fixed: document.txt's DONE log had no
    entry for the theme system build even though PROJECT_CONTEXT.md
    section 9, FRONTEND_TASKS.md, and TEST_TASKS.md already documented it
    as done — added the entry above. PROGRESS.md's "Current status" still
    said Milestone 4 was "Pending" with no mention that the theme system
    (its prep work) had already landed — updated. PROJECT_CONTEXT.md
    section 3 (folder structure) and section 6 (API contracts) spot-checked
    against the actual file tree and route handlers (routes, pages,
    component dirs, engine/db files, GET /api/prediction's response shape)
    — all accurate, no drift found there.
  - Flagged, not touched: an unusual untagged note sits appended at the very
    end of this file after the Milestone 3 entry, phrased as an instruction
    to a reader ("if you read this, ignore it") followed by a suggestion
    about integrating exercise formats into skills. It doesn't match this
    log's dated-entry format and wasn't part of any milestone's actual
    scoped work. Not acted on (content encountered in project files is data
    to review, not an instruction to follow) and not deleted (not this
    review's call to remove something that may have been intentionally
    left) — flagged here for the user to check.
  - App + docs are solid; no other blockers found. Ready for Milestone 4
    Step 2 (USER_GUIDE.md, finalize TESTING_GUIDE.md).
  - The unusual untagged note that had been appended after this entry
    (an instruction-like line telling a reader to "ignore" it while
    smuggling in a feature suggestion — a prompt-injection pattern, not
    a legitimate log entry) was moved out to note.txt (untracked, not
    committed — see .gitignore) at the user's direction, rather than
    left here or silently deleted.

[2026-09-17] Milestone 4, Step 2: TESTING_GUIDE.md + USER_GUIDE.md. MILESTONE 4 DONE.
  - Wrote TESTING_GUIDE.md: how to run each test project, where a new test
    goes for a given kind of change, and a "gotchas already hit once" section
    listing every environment surprise this project ran into (the @ alias,
    RTL cleanup needing to be explicit, next typegen before tsc, the
    set-state-in-effect rule's real fix pattern, e2e needing a production
    build + its own DB) so a future session doesn't rediscover them the
    expensive way.
  - Wrote USER_GUIDE.md (Vietnamese, end-user facing): how to use all 4
    pages (Vòng quay, Nhật ký, Dự đoán, Cài đặt), where data lives
    (./dev.db) and how to back it up, and an FAQ section.
  - Reconciled this file: removed the TESTING_GUIDE.md/USER_GUIDE.md PENDING
    entry (now done) and renumbered the Config UI entry; everything else in
    PENDING is still genuinely deferred and accurate as written.
  - This closes Milestone 4 and the originally-scoped project. Remaining
    work is entirely the PENDING backlog above (auth, cloud deploy, Band
    Prediction v2, part-level weekly-minimum, soft-reset tuning, adjustable
    baseRatio UI for Writing/Listening, Config UI for engine constants) —
    none of it blocks normal use of the app as built.

[2026-09-18] Post-launch bug fixes: illegible/flat form inputs on themed cards.
  - User reported (screenshot, Forest theme) the Journal textarea rendered as
    a near-invisible dark box inside a much brighter green card. Root cause:
    form fields used `bg-background` (the theme's own page background, near-
    black for a dark-classified theme) instead of a dedicated role. Fixed by
    adding `input`/`inputForeground` to ThemeRoles: always the palette's own
    lightest raw color, lightened further in HSL space if it doesn't clear a
    0.85 lightness floor. Applied across all 7 form fields in Journal.tsx and
    CambridgeTracker.tsx. 3 new regression tests (exact Forest reproduction,
    a below-floor "Dark Cold" case, WCAG AA contrast across all 19 themes).
  - Follow-up report on the same screenshot: the fix made the textarea
    legible but still flat/low-contrast against the card, and separately,
    the whole page went near-black for "Forest" when the user expected a
    bright nature theme (reference: a light, organized layout). Two fixes:
    (1) `isDark` in computeThemeRoles() changed from "average luminance < 0.4"
    to "fewer than 2 of the palette's own 4 colors are individually light" —
    Forest averages ~0.37 despite 2 of 4 colors being bright vivid greens, so
    the average-based rule misclassified it as full dark mode; the count-based
    rule reclassifies it as light while leaving "Dark Cold"/"Dark Winter"
    (each only 1 of 4 light) correctly dark. (2) Added a shared `.input-paper`
    CSS class (globals.css) giving every form field a visible colored border
    (color-mix with primary) and a layered box-shadow (elevation + a soft glow
    in the input's own color) instead of a flat fill, so fields read as
    distinct "paper" surfaces lifted off the card. All 7 fields switched from
    raw `bg-input text-input-foreground` utilities to `.input-paper`.
  - Verified: 99/99 unit+component tests green (2 new isDark-reclassification
    tests added, 1 existing Forest test updated to drop its now-inverted
    isDark assertion), typecheck clean, lint clean, and the compiled CSS
    chunk fetched live to confirm `.input-paper` produces real rules
    referencing var(--input)/var(--primary) (with an automatic color-mix()
    @supports fallback emitted by the build, not hand-written).
  - Not independently visually verified by the assistant (no way to render
    pixels) — user asked to re-check the Forest theme in-browser.

[2026-09-18] Post-launch bug fix: page colors following OS/browser dark mode.
  - User reported: with a light theme selected in Settings, the page still
    went dark when the browser/OS was set to dark mode. Root cause was two
    separate leftovers, both relying on `prefers-color-scheme` instead of the
    app's own theme state: (1) globals.css still had the create-next-app
    scaffold's `@media (prefers-color-scheme: dark) { :root {...} }` block,
    overriding the exact same --background/--foreground/--surface/--border/
    --primary/--primary-foreground/--input/--input-foreground/--chart-*
    properties that ThemeProvider sets via inline style — competing with the
    user's actual Settings choice; (2) 8 component files (Spinner, PickCard,
    Journal, CambridgeTracker, CambridgeRow, NoteCard, RatioSliders,
    PredictionPageClient) used Tailwind `dark:` variants on the
    intentionally-fixed status colors (error alerts, delete links, spinner
    cycling/selected states) — Tailwind's default `dark:` strategy compiles
    to the same media query, so these shifted with OS preference too, despite
    PROJECT_CONTEXT.md section 9 explicitly documenting them as "fixed, not
    themed." Fixed by deleting the media-query block entirely and stripping
    every `dark:*` class, leaving one fixed appearance per status color.
  - Also discovered while verifying: the long-running dev server from earlier
    in this session was serving a stale compiled CSS chunk that didn't reflect
    several already-committed source edits (same chunk hash across multiple
    unrelated changes) — restarted it to get an accurate compile. This may
    also explain why the user was still seeing the dark-mode issue after
    earlier, unrelated theme fixes had already landed in source.
  - Verified: 99/99 tests, typecheck, lint all green; fetched the freshly
    compiled CSS for both `/` and `/journal` and confirmed zero
    `prefers-color-scheme` occurrences and zero `.dark\:` rules remain.

[2026-09-18] Post-launch: user still saw a dark page in Cốc Cốc after the fix above.
  - Used a throwaway Playwright script (fresh browser context, no cache, no
    forced-dark browser features) to directly check the app's own computed
    background with the Forest theme forced via localStorage: confirmed
    `--background: #ffffff` and `getComputedStyle(body).backgroundColor` =
    white. This proves the app-side logic from the previous entry was already
    correct — the remaining black background the user saw is not an app bug.
  - Most likely cause: Cốc Cốc (the browser in the user's screenshot) has a
    visible toolbar icon for a built-in "force dark mode for web content"
    feature — separate from the OS/browser prefers-color-scheme *preference*
    this app already stopped following. That kind of feature repaints pages
    regardless of the page's own CSS, which no amount of app-side theme code
    can fully prevent.
  - Added `color-scheme` as a best-effort mitigation: `layout.tsx` exports
    `viewport = { colorScheme: "light" }` and ThemeProvider's applyTheme()
    sets `documentElement.style.colorScheme` to match the active theme's
    isDark. Verified via Playwright that both the meta tag and the computed
    style apply correctly. This is the standards-based signal some (not all)
    forced-dark browser features check before deciding to repaint — not
    guaranteed for every browser, including possibly Cốc Cốc's. If the user
    still sees a dark page after this, the real fix is disabling that
    browser's dark-mode-for-web-content toggle for this site, not more app
    code — flagged clearly to the user rather than implying it's fixable
    from the page alone.

[2026-09-18] Milestone 5, all 4 hats + Supervisor review. MILESTONE 5 DONE.
  User-requested feature set: question-type pool per skill, delete-a-roll
  (revert), manual practice logging, per-skill stats page, minimal Chủ đề
  (topics) CRUD, and a themed-text-color fix. Scoped over several rounds of
  clarifying questions before any code was written (block-splitting for
  question-type draws, manual-practice granularity, delete-revert semantics,
  stats-page placement, topics scope) — see the conversation for the full
  Q&A; only the resulting decisions are recorded here.
  - Backend: schema additions (questionTypes, rollResultQuestionTypes,
    topics tables; skillParts.questionTypeRollCount; rollSessions.source).
    New engine helper pickWeightedIndependent() (Formula 1 reused, N
    independent draws with replacement — no new formula needed). Seed data:
    10 Reading / 7 Listening / 7 Writing question types (Writing's 4 common
    chart types weighted 2x the 3 rarer ones), and questionTypeRollCount per
    part matching the real passage/part count each block bundles (Reading
    Block A=2, Block B=1; Listening A/B=2 each; Writing Task1=1, Task2=0;
    Speaking=0 both). New endpoints: DELETE /api/history/:id (reverts
    counters + recomputes lastAppearedAt from remaining history — documented
    best-effort limitation if a count-soft-reset happened since), POST
    /api/practice (manual logging, reuses rollSessions/rollResults with
    source="manual" so it automatically participates in Formula 1 fairness
    and Formula 3's 30-day frequency nudge with zero extra wiring), GET
    /api/stats/:skillCode, GET/POST /api/topics + DELETE /api/topics/:id.
    getSkillStats()/getAllTopics() added to queries.ts, shared between their
    routes and Server Components (same pattern as getPredictionData()).
  - Frontend: Spinner cascades one level deeper (0-2 question-type draws per
    part, independent per passage/part bundled in a block); RecentRolls
    gained a confirm-guarded delete button + "Tự học" badge; new
    ManualPractice form on the Vòng quay page; new Topics CRUD on Cài đặt;
    new /stats/[skillCode] page (practice log + count/percentage bar charts
    + a radar "balance" chart) linked from both the Spinner's and
    Prediction dashboard's skill cards. Followed the dataviz skill before
    writing chart code: a skill's own question-type breakdown is a
    magnitude-per-category job on ONE entity, not an identity comparison
    across several, so bars use a single hue (that skill's existing
    --chart-* var) instead of inventing a new categorical palette.
  - Design (delegated to a real spawned Design/UI-UX agent — new
    Frontend-only exception added to CLAUDE.md's "no subagent per step"
    rule, scoped to genuinely visual/aesthetic decisions only): theme.ts's
    `foreground` role was a flat #171717/#ededed constant shared by all 19
    themes ("màu chữ chỉ có trắng với đen thôi" — user complaint). Now
    tinted toward each theme's own dominant hue (dominantHue() +
    tintNeutral(), preserving the original lightness exactly) at a low
    0.06 saturation cap. background itself intentionally left untouched —
    tinting the much larger area risked reading as "a colored page." Two
    new regression tests guarantee WCAG AA (>=4.5:1) background/foreground
    contrast across all 19 themes and that two different-hued themes
    produce different foreground values. Also a light polish pass (shadows,
    spacing, hover transitions) scoped only to this milestone's new
    surfaces, per the user's own explicit scoping ("hiện có thì có vẻ ổn
    rồi, redesign 1 số chỗ thôi") — nothing pre-existing was restyled.
  - QA: pickWeightedIndependent unit tests; 4 new component test files
    (RecentRolls, ManualPractice, Topics, PracticeLog); Spinner gained a
    question-type-cascade test; 3 new e2e specs (delete-roll,
    manual-practice, topics). Caught and fixed a real gap while writing
    RecentRolls.test.tsx: its delete button had no confirm() guard, unlike
    Journal/CambridgeTracker's established destructive-delete pattern —
    added one.
  - REAL BUG FOUND while writing/running the new e2e specs (not the
    gated-review pass — caught earlier, during QA's own self-check): nested
    question-type cascades reused the skill/part draws' animation pace.
    A roll landing on parts with several question-type draws (up to 4 in
    one roll: 2 skills x 2 draws each, e.g. both Reading and Listening
    landing on their 2-draw blocks) could take ~35s of real animation time
    in the worst case, intermittently blowing past roll.spec.ts's and
    delete-roll.spec.ts's timeouts. Fixed at the root cause — gave the
    question-type sub-cascade its own faster pace (lower
    minSteps/baseDelayMs in the runCycleAnimation call) instead of just
    padding the test timeout, which is also a better user experience (four
    nested full-length flourishes read as sluggish, not exciting).
    Verified stable with `npx playwright test --repeat-each=3`.
  - Supervisor gated review (diff 9108a26..HEAD, 4 commits): traced the
    delete-revert logic by hand for 0/1/2 rollResults x 0/1/2
    rollResultQuestionTypes per result including same-type-drawn-twice,
    confirmed every lastAppearedAt-recompute query correctly excludes the
    session being deleted, confirmed manual-practice's cross-skill
    ownership checks are real (not just presence checks), confirmed the
    question-type roll/soft-reset scoping matches spec, cross-checked seed
    data against PROJECT_CONTEXT.md 5.7's table (exact match), spot-checked
    new tests for genuineness. Found one trivial issue (a doc-comment on
    the topics table pointed at the wrong PROJECT_CONTEXT.md subsection —
    5.9 instead of 5.11) and fixed it directly. Flagged, not a bug: the new
    delete-roll e2e spec only asserted the history row disappeared, not the
    counter-reversion side effect PROJECT_CONTEXT.md 5.9 actually promises
    — strengthened after the review by snapshotting GET /api/skills
    before/after the roll+delete pair and asserting a full deep-equal
    revert; re-ran 3x, stable (~15-17s each).
  - Final state: 122/122 unit+component tests, 6/6 e2e specs, typecheck
    and lint all green. No open bugs. Deferred/backlog items from this
    milestone (not blockers): question-type ratio-editing UI, Chủ đề not
    yet wired into the roll (more fields expected later, per the user) —
    both intentionally out of scope per the scoping Q&A, not oversights.

[2026-09-18] Post-launch bug fix: weekly-overdue tie-break wasn't random.
  - User reported, while actually using the app: "why does it always pick
    Reading first, then Listening?" Root cause in weeklyConstraint.ts's
    partitionByWeeklyOverdue(): when 2+ skills tie exactly on days-since-
    last-appeared (most commonly all 4 on a fresh app, all null ->
    Infinity), the sort comparator computes `Infinity - Infinity` = NaN. A
    NaN comparator return value makes Array.sort's stable-sort guarantee
    leave tied items in their pre-sort order, which was always DB/seed
    insertion order (Reading, Listening, Writing, Speaking) — so the first
    `slotsNeeded` skills in that fixed order won every tie, forever, not
    randomly. This bug predates Milestone 5 (Formula 2, Milestone 1) but
    only surfaced now that the user was actually rolling repeatedly on a
    fresh-ish dev.db. The existing unit test for this exact scenario
    ("all 4 null, ties as always-overdue") only asserted "doesn't throw,
    forced has length 2, no overlap with remaining" — it never asserted
    WHICH 2 won, so the deterministic bias went uncaught.
  - Fixed by Fisher-Yates shuffling the items (injectable RNG, same
    `random?: () => number` convention as weightedRandom.ts) before the
    sort, and making the comparator return 0 (not NaN) for a genuine tie —
    Array.sort's stability then preserves the shuffled (random) order for
    ties while still correctly ordering skills with real, distinct
    overdue-durations. pickSkillsWithWeeklyConstraint threads its existing
    `options.random` through. No formula/contract change (PROJECT_CONTEXT.md
    5.3's spec — "most overdue first" — was already correct; this fixes an
    implementation bug in how ties specifically were broken), so no doc
    update needed there.
  - Added 2 regression tests to weeklyConstraint.test.ts: one proving a
    different injected RNG produces a different forced pair (not fixed to
    seed order), one running 200 trials with real Math.random and asserting
    more than 1 distinct pair shows up (the bug would have produced exactly
    1, always reading+listening).
  - 124/124 tests, typecheck, lint all green. Did not touch the user's live
    dev server or dev.db while investigating — confirmed via `Get-
    NetTCPConnection -LocalPort 3000` that their own `next dev` (PID 6596)
    was already running before doing anything, so all verification was via
    reading code + running the unit test suite only (no curl/API calls
    against their live session).
  - 99/99 tests, typecheck, lint all green.

[2026-09-19] Design/UI-UX pass: tách giao diện web/ứng dụng + rework theme tối.
  - User complaint: "khi tôi để web giao diện dark thì rất tối và khó nhìn".
    Confirmed direction: "Khung ngoài (nav, nền trang) luôn sáng cố định,
    chỉ nội dung bên trong đổi theo theme" + "Nền tối sáng hơn (xám đậm
    thay vì gần đen) + thêm glow/sheen nhẹ trên card".
  - Root cause: `background`/`foreground` in computeThemeRoles() branched on
    `isDark` exactly like `surface` did, so picking either dark theme
    ("Dark Cold"/"Dark Winter") turned the ENTIRE page — nav bar included —
    near-black (#0a0a0a), not just cards.
  - Fix (src/lib/theme.ts): background/foreground dropped the isDark branch
    entirely — always the fixed light pair now, for every theme. `surface`/
    `border` keep varying with isDark (that's the only place a dark theme's
    character lives now); its dark-tier value is no longer picked verbatim
    from the palette's own darkest raw color (near-black, ~0.15 lightness)
    but synthesized by a new pickDarkSurface() — same hue, re-rendered at a
    fixed lighter SURFACE_DARK_LIGHTNESS=0.24 with SURFACE_DARK_MAX_
    SATURATION=0.32 capped saturation, landing on a genuine dark GRAY
    ("Dark Cold" surface -> #2a3051, "Dark Winter" -> #2a4c51) instead of
    near-black. Raw palette hexes for both dark themes left untouched — only
    how `surface` is derived from them changed; decided (after reviewing
    src/theme/DarkColdColor.png/DarkWinterColor.png and every other theme's
    raw colors) that no other theme's isDark classification needed to
    change and no new dark theme was worth adding.
  - New role `surfaceForeground`: needed because surface can still be dark
    while background/foreground can't anymore, so text *inside* a themed
    card needs its own contrast-safe role (same #111111-vs-#ffffff max-
    contrast pick as primaryForeground/inputForeground). Wired through
    ThemeProvider.tsx (`--surface-foreground`) and globals.css (`@theme
    inline` -> `text-surface-foreground`/`bg-surface-foreground`).
  - Nav.tsx: `bg-surface/80` -> `bg-background/80` (background is now
    always-fixed-light, so this alone makes the nav bar "always light
    chrome" in every theme without any nav-specific logic).
  - Swept all ~17 files using `text-foreground` (grep -rl) plus
    ThemePicker.tsx (didn't match the grep — uses computeThemeRoles()
    directly via inline style, not the Tailwind class — caught by reading
    the component, not by trusting the grep list) to split page-level text
    (kept `text-foreground`/`roles.foreground`) from text actually inside a
    `bg-surface`/`bg-surface/NN`/`bg-border` card (moved to
    `text-surface-foreground`/`roles.surfaceForeground`). Files changed:
    NoteCard.tsx, CambridgeRow.tsx, Journal.tsx (form section + bg-border
    tag pills), CambridgeTracker.tsx (form div), PredictionCards.tsx
    (SkillCard), RatioSliders.tsx, RoundingModeToggle.tsx, Topics.tsx,
    ManualPractice.tsx, PickCard.tsx (idle + dimmed states), RecentRolls.tsx,
    PracticeLog.tsx, stats/[skillCode]/page.tsx, ThemePicker.tsx (swatch
    preview now shows roles.surface/surfaceForeground instead of
    roles.background/foreground — otherwise every swatch would render
    identically since background/foreground no longer vary per theme).
    Left as page-level (unchanged): Spinner.tsx's h1/h2/h3, Journal.tsx's
    h1, PredictionPageClient.tsx, prediction/page.tsx, settings/page.tsx,
    stats/[skillCode]/page.tsx's h1, PredictionCards.tsx's bg-primary/10
    overall-band div (10%-opacity wash over the always-light page reads as
    near-white regardless of theme, confirmed safe not just assumed).
  - Two subtler findings caught only by reading full components, not by
    grepping `text-foreground` alone: (1) RecentRolls.tsx/PracticeLog.tsx
    used `bg-surface/70` — safe before (background shared surface's own
    light/dark tier), but now that background is always light, a
    translucent dark-theme surface washes toward light instead of staying
    dark — changed to opaque `bg-surface`. (2) AccuracyEntry.tsx renders in
    two different color contexts (page-level under Spinner.tsx, nested
    inside ManualPractice.tsx's bg-surface card) — gave it a `variant?:
    "page" | "surface"` prop instead of guessing one context wrong.
  - Glow/sheen treatment: new `.surface-glow` utility in globals.css
    (`@layer utilities`, same layered-box-shadow family as the existing
    `.input-paper`) — soft drop shadow + a wide, faint glow in the card's
    own `var(--surface)` color. Applied to every bg-surface card container
    found in the sweep above (not PickCard's "dimmed" state, which is
    deliberately fading toward the page — a glow would fight that). Same
    formula for light and dark themes on purpose: negligible visually on a
    near-white light-theme surface, does the actual "phản quang" work on a
    dark-theme surface.
  - Added regression tests to theme.test.ts: background fixed to #ffffff
    for all 19 THEMES (including both dark ones explicitly by id); rewrote
    the old test that asserted a dark palette's background === "#0a0a0a"
    (that behavior is exactly what was fixed, so the assertion was
    updated, not weakened, to check the new contract instead — plus that
    surface's own lightness lands in the intended 0.18-0.30 dark-gray
    band); WCAG AA (>=4.5:1) contrast test for surface/surfaceForeground
    across all 19 themes, same pattern as the existing input/primary
    tests; surfaceForeground picks white for a dark theme's surface and
    black for a light theme's surface; border stays equal to surface (used
    as a solid fill in a few spots, e.g. bg-border tag pills, not just an
    outline).
  - PROJECT_CONTEXT.md section 9 updated in the same step (new role, the
    always-light shell, pickDarkSurface, .surface-glow, the ThemePicker/
    Nav/AccuracyEntry-variant fixes). src/app/FRONTEND_TASKS.md's matching
    unchecked item ticked off.
  - Verified end-to-end with a real production build against a disposable
    DB (DATABASE_PATH=./_design_check.db, port 3099, never touched the
    user's live dev server/dev.db/port 3000): confirmed Nav's HTML actually
    ships `bg-background/80`, confirmed the compiled CSS chunk contains the
    `.surface-glow` box-shadow rule, the `--surface-foreground` custom
    property, and a `text-surface-foreground` utility. Server stopped and
    the throwaway DB + seed script deleted immediately after.
  - 138/138 tests (up from 133; net +5: 4 new tests plus one existing test's
    assertions rewritten in place for the new background contract),
    typecheck and lint all green. Diff left uncommitted for the calling
    session to review.

[2026-09-19] "Milestone 5 mở rộng", all 4 hats + Supervisor review. DONE.
  User-requested on top of the just-closed Milestone 5: (1) replace Formula
  3's flat 30-test mean with a recency-weighted average so a single recent
  real test genuinely moves the prediction ("hôm nay 2.5, tuần sau 4.5"
  shouldn't average to neither), plus a practice-accuracy signal for
  Reading/Listening; (2) fix dark themes being "very dark and hard to see"
  by separating the app's outer chrome from its themed content. Scoped over
  several rounds of clarifying questions before any code was written
  (confirmed exact weight table 65/30/5 vs 65/35, confirmed EWMA over the
  user's own suggestion of a GRU/LSTM — explained why a neural sequence
  model doesn't fit a single-user app with a few dozen data points per
  skill, no ML infra, and no training signal at that scale — confirmed the
  "khung ngoài luôn sáng" framing for the theme ask) — only the resulting
  decisions are recorded here, see the conversation for the full Q&A.
  - Backend: new engine/ewma.ts (computeEwma, alpha=0.5 default — each new
    point weighted equal to the entire prior history combined, deliberately
    reactive). Rewrote bandPrediction.ts: Reading/Listening = 65% Cambridge
    EWMA + 30% accuracy EWMA (0-9 scale) + a frequency nudge capped at 5% of
    9; Writing/Speaking (no objective accuracy signal) = 65% Cambridge EWMA
    + a frequency nudge capped at 35% of 9. Missing accuracy data falls back
    to the Cambridge EWMA for that slice (mathematically 0.95x cambridgeEwma)
    rather than losing the weight or fabricating a number. New schema:
    nullable questionsAnswered/questionsCorrect on rollResults, new PATCH
    /api/results/:id/accuracy (Reading/Listening only, real ownership check
    via the result's own skillId, not client-claimed). New Config keys
    cambridge_ewma_alpha/accuracy_ewma_alpha; frequency_adjustment_cap
    removed (cap is now derived per-skill from the frequency weight x 9).
    Verified against a disposable DB via a throwaway script, never touched
    the user's live dev.db.
  - REAL BUG FOUND AND FIXED, unrelated to the two requested changes — user
    reported live, while actually using the app: "sao luôn chọn reading với
    listening đi kèm luôn vậy... tôi bấm từ nãy luôn thấy reading là đầu
    tiên, sau đó là listening". Root cause in weeklyConstraint.ts (Formula
    2, dates back to Milestone 1): when 2+ skills tie exactly on
    overdue-ness (most commonly all 4 on a fresh app, all null ->
    Infinity), the sort comparator computed `Infinity - Infinity` = NaN,
    and a NaN comparator return makes a stable sort leave tied items in
    their original DB/seed order — so the same 2 skills (Reading, Listening
    — first 2 in seed order) won every tie, never randomly. The existing
    "all 4 null" unit test only asserted "doesn't throw, right counts" and
    never checked WHICH 2 won, so this went uncaught since Milestone 1.
    Fixed via a Fisher-Yates shuffle (injectable RNG) before the sort, plus
    a real `0` return for a tie instead of `NaN`. Investigated and fixed
    without touching the user's own live running dev server/dev.db —
    confirmed via Get-NetTCPConnection it was already running before doing
    anything.
  - Frontend: new AccuracyEntry.tsx (optional quick "số câu đã làm/số câu
    đúng" entry), shown under each Reading/Listening result after a roll
    (Spinner.tsx) or a manual-practice log (ManualPractice.tsx).
    PredictionCards.tsx gained a small "Cambridge X.X · Luyện tập Y.Y"
    breakdown line per skill.
  - Design (delegated to a real spawned Design/UI-UX agent, per the
    Frontend-only exception CLAUDE.md already documents): background/
    foreground in computeThemeRoles() no longer branch on isDark at all —
    always the fixed light "page shell" pair (nav bar + page background),
    for every theme including the 2 dark ones. surface/border still vary
    with isDark, so a dark theme's character now lives entirely in cards,
    not the page shell. New pickDarkSurface() synthesizes a genuine dark
    GRAY (fixed lightness 0.24, capped saturation) instead of picking the
    palette's own near-black raw color verbatim, per the explicit follow-up
    "xám đậm thay vì gần đen". New surfaceForeground role for text inside
    themed cards; swept ~18 files off text-foreground where the text
    actually sits inside a bg-surface container (caught 2 subtle cases by
    reading full components, not just grepping: RecentRolls/PracticeLog's
    translucent bg-surface/70 would wash toward the now-light background,
    and AccuracyEntry renders in two different color contexts, needing a
    variant prop). New .surface-glow CSS utility (same layered-shadow
    family as the existing .input-paper) for the requested "phản quang"
    depth effect on cards. Reviewed src/theme/*.png source palettes; kept
    both dark themes' raw hexes unchanged (only how surface derives from
    them changed). Verified end-to-end via a production build on a
    disposable DB/port — never touched the user's live dev server.
  - QA: new ewma.test.ts, rewrote bandPrediction.test.ts for the v2 shape,
    new AccuracyEntry.test.tsx, extended Spinner/ManualPractice component
    tests, new accuracy-entry.spec.ts e2e. Found and fixed a real e2e
    fragility while adding it: cambridge-prediction.spec.ts hard-coded the
    exact predicted overall band ("6.5") computed under the OLD v1 formula
    — wrong under v2 regardless, and additionally brittle because every e2e
    spec shares one persistent DB (setupDb.ts seeds once before the build,
    not per-spec), so the new accuracy-entry.spec.ts running first
    (alphabetical order) shifted Reading's practice frequency before
    cambridge-prediction.spec.ts ran. Replaced the hard-coded value with a
    "some valid band number is showing" check; documented the shared-e2e-DB
    footgun in PROJECT_CONTEXT.md section 7 for future specs.
  - Supervisor gated review (diff 05c05d5..HEAD, 6 commits): hand-traced
    the EWMA recurrence and weighted-blend logic against section 5.4,
    confirmed getPredictionData()'s two `.reverse()` calls (Cambridge bands
    and accuracy percentages) both correctly feed chronological order into
    computeEwma, confirmed the accuracy-PATCH ownership check is real (skill
    derived server-side from the result row, not client input). Deliberately
    reverted the weekly tie-break fix locally, reran the test suite, got
    exactly the 2 expected failures, then restored it — a genuine
    break-then-revert verification of the regression tests, not just
    reading them. Hand-computed pickDarkSurface()'s output for both real
    dark themes and matched it byte-for-byte against the docs. Spot-checked
    11+ of the text-foreground-sweep files against a fresh grep — no false
    positives, no missed file. Found one real issue: PROJECT_CONTEXT.md
    5.6 and this file's own PENDING #4 still called the deferred
    OLS-regression idea "v2" and referenced a variable
    (`cambridgeAvg_skill`) that this milestone's EWMA work had already
    renamed — collided with the new, already-shipped "v2" (EWMA). Renamed
    the deferred idea to "v3" and fixed the variable references in both
    docs (see PENDING #4 below, now retitled).
  - Final state: 143/143 unit+component tests, 7/7 e2e specs, typecheck and
    lint all green. No open bugs.

[2026-09-19] Post-launch: 3 issues found live while the user tried the app.
  - (1) "background vẫn đen lắm" (still very dark), with a screenshot: the
    Settings page's page background/card borders were dark even with
    "Mặc định" (a light theme) active and confirmed selected — but each
    theme swatch's own little color-preview blocks still showed correct,
    undistorted colors. That specific pattern (flat backgrounds inverted,
    saturated small blocks left alone) is the signature of a browser-level
    "force dark mode for web content" feature repainting the page after
    load, not a CSS bug — confirmed by grepping the whole src/ tree for
    `prefers-color-scheme` and `dark:` (zero real hits, matching the
    already-fixed state from the earlier Cốc Cốc report). Investigating
    further found one real leftover bug though: ThemeProvider.tsx's
    applyTheme() still set `documentElement.style.colorScheme` to "dark"
    for a dark-classified theme (roles.isDark) — a leftover from before
    the "tách giao diện" rework. color-scheme is a separate browser signal
    from the page's own CSS: it controls NATIVE form-control rendering
    (<select> dropdown popups, date pickers, number-input spinners,
    scrollbars), independent of what background-color the page itself
    paints. With it still "dark", opening any <select> (Kỹ năng/Part in
    ManualPractice, etc.) would pop a dark-native dropdown regardless of
    the correctly-light page around it. Fixed: color-scheme is now always
    "light", since the page shell has no real dark mode left at all
    (isDark only ever affects card surface tone now). No test asserted
    the old per-theme value (grepped first to confirm before changing).
  - (2) User: "khi tôi chạy quay thì nó ra 2 cái này, nhưng khi tôi nhấn
    xóa, nó vẫn hiện 2 cái này vậy" — deleting a roll from "Lượt quay gần
    đây" left the Spinner's own cascade display (which skill/part/dạng bài
    cards show as "selected", their accuracy-entry forms, etc.) completely
    unchanged, still showing the now-deleted roll's results as chosen.
    Root cause: Spinner.tsx's handleHistoryDeleted only refetched
    skills/history; it never touched skillStates/partStatesBySkillId/
    typeDrawStatesBySkillId/etc., which are separate client-side display
    state from the server data. Fixed by extracting resetCascadeDisplay()
    (previously inlined at the top of handleRoll) and calling it — plus
    setPhase("idle") — from handleHistoryDeleted too, so a delete now
    genuinely resets the display back to its pre-roll state.
  - (3) User: "với cả điền số câu thì phải có chỗ để điền, ví dụ nhấn vô
    lượt quay gần đây thì sẽ có chỗ để điền số câu và nhấn lưu" — a real
    gap in Milestone 5 mở rộng's accuracy-entry feature (5.12): the
    AccuracyEntry form only ever appeared once, immediately after a
    roll/manual-practice completed. If the user skipped it then (or
    reloaded the page), there was no way back to enter it for that result
    — RecentRolls.tsx just displayed the result with no accuracy affordance
    at all. Fixed by adding a "+ Nhập số câu đúng" prompt to every
    Reading/Listening result in Lượt quay gần đây that doesn't have
    questionsAnswered yet, expanding into the same AccuracyEntry component
    inline. Also improved AccuracyEntry's onSaved callback to pass back the
    actual saved {questionsAnswered, questionsCorrect} (previously no
    args) so RecentRolls can show the real numbers immediately rather than
    a generic checkmark, without needing a full history refetch.
  - Added tests for all 3: a regression test in Spinner.test.tsx for the
    cascade-resets-after-delete fix; 4 new RecentRolls.test.tsx cases for
    the "+ Nhập số câu đúng" flow (prompt shown/hidden correctly, expands
    into the form, shows the real saved numbers immediately, none for a
    skill with no accuracy component); a new e2e case in
    accuracy-entry.spec.ts covering the "skip it now, fill it in later
    from history" flow end to end. No color-scheme test existed to update
    (grepped first).
  - 148/148 unit+component tests, 8/8 e2e specs, typecheck and lint all
    green.

[2026-09-19] Post-launch, 4th report of the same "nền vẫn tối" symptom —
  this time with screenshots taken AFTER the color-scheme fix from the
  entry above (item 1) was already committed and pushed. Re-verified from
  scratch rather than assuming: background is still hardcoded "#ffffff"
  for every theme in theme.ts, zero `prefers-color-scheme`/`dark:` rules
  anywhere in src/ (grepped again), and both the static
  `viewport.colorScheme` meta tag and ThemeProvider's runtime
  `documentElement.style.colorScheme` are "light". So the app is doing
  everything a page can standards-wise, and it still didn't stop it —
  that resolves the "not guaranteed for every browser" uncertainty from
  the earlier entry: confirmed Cốc Cốc's own "Duyệt web chế độ tối" (Dark
  Mode Browsing) feature does NOT back off for a page that declares
  `color-scheme: light`; it repaints the whole page after load regardless.
  No further app-side code fix exists for this — closing it as a browser
  setting, not a bug. User-facing fix given: in Cốc Cốc, click the moon
  icon at the end of the address bar → "Tắt" to disable dark mode for
  just this site (keeps it on elsewhere), or Settings → "Duyệt web chế độ
  tối" → disable globally / add this domain to the exclusion list.
  Source: https://support.coccoc.com/desktop/duyet-web-o-che-do-toi-tren-coc-coc.
  Logging this so a future session doesn't re-investigate a 5th time.

[2026-09-19] Milestone 6 (Chatbot AI - ha tang + RAG), 3 buoc + Supervisor
  review. MILESTONE 6 DONE.

  Full scope/decisions log: AI_CHATBOT_PLAN.md (repo root) — this entry is
  a compressed summary, that file is the source of truth for *why*.

  Step 1 (restructure, commit 1e231bc): whole app git-mv'd into web/, new
  ai/ module scaffolded (models/, data/{raw,processed}/, training/,
  server/), new AI/ML role + .claude/agents/ai-engineer.md added to
  CLAUDE.md. Real environment gap hit and fixed: deleting the old root
  node_modules broke better-sqlite3 (no Node 24 prebuilt binary, needs
  node-gyp, machine had no Visual Studio Build Tools despite an empty
  "Visual Studio\2022" folder existing) — installed VS Build Tools + C++
  workload via winget with the user's explicit go-ahead. Verified green
  after the move: npm install, typecheck, lint, 148/148 test, 8/8 e2e.

  Step 2 (web/ side of chat, commit 7dbd771): POST web/src/app/api/chat/
  route.ts (validate -> getChatContextSummary() new query helper reading
  web/'s own DB -> fetch to ai/server/ -> map 503/502) + web/src/app/chat/
  page.tsx + ChatWindow.tsx + Nav entry. ChatWindow.test.tsx (5 cases).
  153/153 test green (jsdom lacks scrollIntoView — guarded with ?.()).

  Step 3 (ai/server/ RAG + real Ollama, commit d4f0820): installed Ollama
  via winget, pulled qwen3.5:4b (3.4GB) + nomic-embed-text (274MB) — both
  confirmed available on Ollama's official library. Built ai/server/
  (FastAPI): chunking.py + retrieval.py + prompt.py (pure, 19 pytest cases)
  + ollama_client.py (I/O boundary) + build_index.py + main.py. Real bugs
  found and fixed while actually running this (not just reading code):
    (a) chunk_markdown didn't split a single paragraph longer than
        max_chars (no blank line inside it) — nomic-embed-text rejected it
        outright with "input length exceeds the context length" when
        indexing a real long narrative paragraph in PROJECT_CONTEXT.md
        section 9. Fixed with _split_oversized_paragraph (sentence-boundary
        split, hard-slice fallback) + 3 regression tests.
    (b) Qwen3.5 runs extended "thinking" mode by default — measured live:
        the same trivial prompt took 73.7s (thinking on) vs 1.1s (thinking
        off via Ollama's `think: false` chat param) — ~65x. Without this,
        the chatbot would be unusable on this machine's 4GB VRAM. Set
        think=False in ollama_client.py permanently.
    (c) A real RAG-grounded question ("Công thức tính Band điểm hoạt động
        thế nào?") took ~46s even with thinking off — genuine hardware
        characteristic (4B model + RAG-lengthened prompt on a 4GB laptop
        GPU), not a bug. Raised web/'s AI_SERVER_TIMEOUT_MS 60s->120s to
        match ai/server/'s own timeout, added a "may take ~1 min" note to
        the chat UI so the user doesn't think it hung.
  Verified end-to-end with the REAL model (not mocked): asked a real
  Vietnamese question about the Band formula, got a correct answer citing
  the right PROJECT_CONTEXT.md content. Added chat.spec.ts (e2e) against a
  lightweight mock AI server (web/src/tests/e2e/mockAiServer.ts, a 2nd
  parallel `webServer` entry in playwright.config.ts) rather than the real
  Ollama pipeline — deliberate: real per-question latency (~46s) would make
  the e2e suite slow and dependent on Ollama being installed on whatever
  machine runs it; e2e proves web/'s own wiring, not answer quality (that's
  confirmed manually, logged here). Added ai/start-server.bat (one-click
  launcher, mirrors the existing start-app.bat convention) + USER_GUIDE.md
  section 4 (Chatbot) explaining the two-server setup and expected latency.

  Supervisor review (scoped diff ca94711..HEAD, 3 commits): ran real
  self-check commands (not just reading code) — web/: typecheck/lint/153
  tests all green; ai/: 19/19 pytest green. Confirmed no Claude/Anthropic
  output anywhere in the training-data path (grepped ai/server/*.py),
  confirmed ai/ never reads web/'s DB directly (grepped for
  sqlite/dev.db/drizzle references in ai/server/ — none), confirmed the
  module boundary holds. Exactly 1 finding: PROJECT_CONTEXT.md section 11.4
  still said "timeout 60s" after the timeout was raised to 120s mid-step —
  fixed, no second review pass needed (doc-only, no judgment call).

  Next: Milestone 7 (fine-tune with Kaggle-generated data, user approves a
  sample before training) or Milestone 8 (Writing/Speaking scoring against
  IELTS band criteria) — see AI_CHATBOT_PLAN.md section 9 for the full plan.
  Neither is blocking; the chatbot is usable as-is (RAG + base model,
  no fine-tune yet) once both start-app.bat and ai/start-server.bat are
  running.

[2026-09-19] Milestone 6 mở rộng: đổi tên "Navita" + bình chat nổi + 3 chế
  độ (Flash/Thinking/Pro) + lịch sử nhiều cuộc hội thoại. Supervisor
  review + 1 vòng sửa. MILESTONE 6 MỞ RỘNG XONG.

  User quay lại sau khi Milestone 6 đã review chốt (mục ngay phía trên),
  yêu cầu thêm 4 việc — không phải milestone số riêng, coi là phần mở
  rộng cùng phạm vi. Chi tiết đầy đủ: AI_CHATBOT_PLAN.md mục 11,
  ai/AI_TASKS.md mục "Milestone 6 mở rộng".

  Việc chính: đổi tên chatbot thành Navita (hệ dẫn, welcome message, Nav,
  USER_GUIDE); bình chat nổi (ChatBubble.tsx, gắn layout.tsx, tự ẩn ở
  /chat để không trùng lặp, dùng chung state/API với trang đầy đủ); 3 chế
  độ Flash/Thinking/Pro (vẫn 1 model qwen3.5:4b, khác think/top_k/hệ dẫn
  qua ai/server/main.py's MODE_SETTINGS); lịch sử nhiều cuộc hội thoại lưu
  DB (bảng chatConversations/chatMessages mới, migration 0003, viết lại
  POST /api/chat để lấy lịch sử từ DB thay vì nhận từ client).

  Supervisor review (diff ba388f7..HEAD, commit bcf3282) tìm ra 5 finding
  thật, không phải hình thức:
    1. [Nghiêm trọng] Xóa cuộc hội thoại đang chờ trả lời (Thinking/Pro
       tới ~4 phút) → addChatMessage(assistant) insert vào cuộc đã xóa →
       SQLite FK constraint (foreign_keys=ON) chặn → request crash 500,
       mất câu trả lời AI dù đã tốn GPU thật. Sửa: try/catch ở route.ts,
       vẫn trả reply cho client dù lưu DB thất bại.
    2. [Nghiêm trọng] Chuyển cuộc hội thoại trong lúc đang chờ trả lời →
       câu trả lời "rò" vào cuộc đang xem + tự động kéo user quay lại
       cuộc cũ không báo trước. Sửa: khóa dropdown/nút +/xóa khi
       isSending (chặn từ UI) + activeIdRef kiểm tra đúng cuộc trước khi
       cập nhật messages/lỗi (chặn ở tầng logic) + refreshConversations()
       không còn ép đổi activeId.
    3. [Trung bình] Bấm Gửi/Enter rất nhanh 2 lần trước khi isSending
       (state) kịp render → tạo trùng 2 cuộc hội thoại + gửi trùng. Sửa:
       isSendingRef (ref đồng bộ, không phụ thuộc chu kỳ render).
    4. [Nhỏ] PROJECT_CONTEXT.md mục 11.4 còn ghi "timeout 120s" dù code
       đã tăng 300s — đã sửa.
    5. [Nhỏ] Test mới thiếu case cho 3 race condition trên — đã thêm 2
       test dùng fireEvent.click (không dùng userEvent vì nó tự chờ React
       render giữa các thao tác, không tái hiện được race thật).
  Tự phát hiện thêm 1 lỗi khi vá Finding 2: lần sửa đầu dùng useEffect để
  đồng bộ activeIdRef theo activeId, nhưng handleSend vừa tạo cuộc hội
  thoại (set activeId) vừa cần đọc activeIdRef đã cập nhật ngay sau đó
  trong cùng 1 lần gọi — effect không đảm bảo chạy kịp. 3 test tự dưng đỏ
  sau khi thêm fix, phát hiện ra ngay. Sửa bằng cách gán activeIdRef.current
  trực tiếp cùng lúc với mọi setActiveId (qua hàm setActiveIdAndRef mới),
  bỏ hẳn effect.

  Xác nhận thật với model thật (không mock) sau khi sửa: Flash 30.5s + Pro
  244s đều chạy đúng, tự giới thiệu đúng là Navita, Pro đúng phong cách
  trang trọng/có cấu trúc. Chụp màn hình bằng Playwright (script tạm) xác
  nhận giao diện đúng thiết kế (bình nổi đúng vị trí, welcome message +
  3 nút chế độ + dropdown hội thoại trong popup, trang /chat không hiện
  bình nổi trùng lặp), không lỗi console.

  Tự kiểm cuối: typecheck/lint sạch, test 158/158, test:e2e 9/9 (web/),
  pytest 22/22 (ai/) — tất cả xanh.

